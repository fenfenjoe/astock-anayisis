# REQ-006: 信号质量统计区分"已执行样本"与"未执行模拟样本"，未执行信号不入账户级 P&L

- **创建时间**: 2026-09-03 16:35
- **优先级**: P2
- **影响范围**: 收盘复盘
- **状态**: OPEN

## 开发流程（强制）

> 本需求实施**必须走 superpowers 流程**。不要在生成需求后直接裸写代码。

实施时严格按以下顺序执行：

```
1. brainstorming   — 探索方案空间，不跳入单一实现
2. writing-plans   — 写出详细实施计划（含文件清单、API/数据结构变更）
3. TDD             — 先写测试，再写代码
4. executing-plans — 按计划逐步实施
5. code-review     — 自查 + 修复
```

**铁律：**
- 禁止跳过 TDD — 没有测试的需求不得推进到 IMPLEMENTED
- 禁止跳过 brainstorming — 不假思索的实现 = 返工
- 涉及 A 股数据的，仍走 `a-stock-data` skill

## 需求描述

2026-09-03 复盘发现信号质量统计口径缺陷（已沉淀经验到投资经验.md 信号设计⑥）：

- **现象**：SIG-20260901-06-盘中（农业 159825）9/1 触发但**用户未执行**（9/1 复盘执行质量 D，无实际持仓），9/3 T+3 到期被 `settle_due_signals` 按假设执行结算为 miss -231.8 元 → 追踪库累计 P&L 由 +146.7 转 -85.1、胜率 75%→60%、信号期望价值转负。
- **问题**：未执行信号的实际账户 P&L 为 0（未执行反而避免 -2.6% 亏损，9/1/9/2 复盘已确认），其结算 miss 是**信号质量评估**（方向/目标达成率）而非**账户损益**。当前 `settle_due_signals` 对所有 status ∈ {triggered/executed} 的到期信号统一按假设执行结算，未区分是否真实执行 → 账户级指标（累计 P&L/胜率/期望价值）被"未执行的失败信号"污染，反向惩罚正确的执行缺失判断。
- **改进**：信号质量统计区分两类样本——① 已执行样本（status_history 含 executed/部分执行，记账户 P&L）；② 未执行模拟样本（仅 triggered 无 executed，只记 outcome 用于方向/目标达成率评估，P&L 单列"假设口径"不并入账户级统计）。

## 期望结果

- `lib/signal_tracking.py`：`settle_due_signals` 对 status=triggered 且 status_history 无 executed/partial_executed 记录的信号，结算时标注 `simulated=true`；`update_aggregation` 聚合区分 `total_pnl_amount`（仅已执行）与 `simulated_pnl_amount`（未执行模拟）两个字段
- `lib/signal_quality.py`：`generate_quality_dashboard` 的 signal_expected_value/win_rate 等账户级指标仅基于已执行样本；simulated 样本单独披露
- `config/signal_tracking.json`：schema 兼容（旧记录无 simulated 字段视为已执行或按 status_history 推断）
- 复盘报告/每日信号"信号收益追踪"节并排披露：已执行 P&L + 未执行模拟 P&L（标注假设口径）

## 测试交付物

> 以下清单在 TDD 步骤（auto_req_implement 第 3 步）中填写，在第 3.5 步门禁中验证。

- [ ] 测试文件: `harness/automation/tests/test_REQ_006.py`
- [ ] 被测模块: `harness/automation/lib/signal_tracking.py` + `lib/signal_quality.py`
- [ ] TDD 门禁通过: `python -m pytest harness/automation/tests/test_REQ_006.py -v`
- [ ] 回归测试: 全量 `python -m pytest harness/automation/tests/ -v` 通过

**测试覆盖要求：**
- `settle_due_signals`：triggered+无 executed → simulated=true；triggered+有 executed → simulated=false；executed → simulated=false；P0 不结算不变
- `update_aggregation`：simulated 记录 P&L 入 simulated_pnl_amount 不入 total_pnl_amount
- 历史记录（无 simulated 字段）兼容推断

## 约束/注意事项

- 经验库已沉淀（投资经验.md 信号设计⑥），实施以 lib 落地为主
- 涉及金融计算 → `auto_fix_eligible=false` 类改动需 code-review 严格
- 9/3 农业 miss -231.8 为最小复现样本（signal_tracking.json 中 SIG-20260901-06-盘中 记录）

## 处理记录

| 时间 | 操作 | 备注 |
|------|------|------|
| 2026-09-03 | 创建 | 9/3 复盘发现：未执行信号按假设结算污染账户级指标（农业 miss -231.8 致累计 P&L 转负/胜率 60%/期望价值 -17.02），实际账户无此亏损——需区分已执行/未执行模拟样本 |
| 2026-09-09 | brainstorming | 方案A（最小改动，选中）: 在 signal_tracking.py 加 `is_simulated(sig)` 推断函数（显式 simulated 字段优先，否则按 status_history 是否含 executed/partial_executed 推断）；settle_due_signals 结算时标注 simulated；update_aggregation 将 pnl 分流 total_pnl/simulated_pnl，win_rate 仅基于已执行；signal_quality.generate_quality_dashboard 分离 executed/simulated 样本，账户级指标仅用 executed。方案B（完整重构）: 新建独立 simulated 聚合视图 + 历史记录 schema 迁移脚本，改动面大、回滚复杂。选择方案A 理由: 纯函数改动集中、满足全部期望结果、旧记录无需物理迁移（代码推断兼容）、回滚=git revert 单文件 |
| 2026-09-09 | writing-plans | 实施计划: 1) lib/signal_tracking.py - 新增 `is_simulated(sig)`（simulated 字段优先，否则 status_history 含 executed/partial_executed → False）；settle_due_signals 结算时按结算前状态标注 `simulated`（status∈{triggered,open} 或无执行记录→True）；_empty_agg 加 simulated_pnl 字段；_update_bucket 对 settled 信号分流 pnl（simulated→simulated_pnl，不参与 win_count）；aggregates 新增 simulated_pnl_amount，total_pnl_amount 语义改为仅已执行，win_rate/lose_count 仅基于已执行样本。2) lib/signal_quality.py - 新增 `split_executed_simulated(settled)`；generate_quality_dashboard 账户级指标（expected_value/盈亏比/最大亏损/方向准确率/目标达成率/平均达标天数）仅用 executed，simulated 单独披露（simulated_count/simulated_pnl/simulated_expected_value/simulated_target_hit_rate）。3) prompts/auto_evening_review.md - 7.6.3 累计追踪统计表增加"未执行模拟P&L（假设口径）"行，7.6.4 增加 simulated 披露行。数据结构变更: signals 记录新增可选字段 simulated(bool)，聚合 bucket 新增 simulated_pnl。测试策略: 新建 tests/test_REQ_006.py 覆盖 settle 标注/聚合分流/历史推断 + 全量回归 pytest。回滚: git checkout 相关文件 |
| 2026-09-09 | TDD | 测试用例: 1) is_simulated 显式字段优先/status_history 推断/status 推断/旧记录推断 → 预期各分支正确; 2) settle_due_signals triggered+无executed→simulated=true, triggered+有executed→false, executed→false, P0不结算不变, 结算仍计算outcome/pnl → 预期如上; 3) update_aggregation simulated pnl→simulated_pnl_amount 不入 total_pnl_amount, 已执行→total, 混合分流, 历史记录推断, win_rate 仅已执行 → 预期如上; 4) split_executed_simulated + generate_quality_dashboard 账户级指标仅已执行、simulated 单独披露 → 预期如上。首跑: 红条（ImportError: is_simulated 不存在），测试有效 |
| 2026-09-09 | executing-plans | 已修改: lib/signal_tracking.py（is_simulated 新增 + settle_due_signals 标注 simulated + _empty_agg/_update_bucket 分流 + update_aggregation 新增 simulated_pnl_amount）、lib/signal_quality.py（split_executed_simulated + generate_quality_dashboard 账户级仅已执行 + simulated 披露）、prompts/auto_evening_review.md（7.6.3/7.6.4 并排披露已执行/模拟口径）；新建 tests/test_REQ_006.py（24 条用例）。验证: REQ-006 测试 24/24 绿；全量回归 442 通过 1 跳过；真实数据 sanity: 农业 miss -231.8 → simulated，累计已执行 P&L 恢复 +146.7、胜率 75%（未被污染前口径） |
| 2026-09-09 | code-review | 自查 7/7 通过（语法/JSON 解析、引用路径、流水线依赖、幂等性、回滚可行性、无 A 股数据取数、prompt 表格对齐）; 测试 442 通过/1 跳过（预存在）; 最终判定: PASS |
