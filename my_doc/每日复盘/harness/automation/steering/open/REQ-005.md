# REQ-005: 投资经验.md 压缩合并（623 行 >500 强制线）+ 重复章节/条目去重 + 北向数据断供标注

- **创建时间**: 2026-09-02 12:52
- **优先级**: P1
- **影响范围**: 经验健康, 早盘分析, 盘中检查, 收盘复盘
- **状态**: CLOSED

## 开发流程（强制）

> 本需求实施**必须走 superpowers 流程**。不要在生成需求后直接裸写代码。

实施时严格按以下顺序执行：

```
1. brainstorming   — 探索方案空间，不跳入单一实现
2. writing-plans   — 写出详细实施计划（含文件清单、API/数据结构变更）
3. TDD             — 先写测试，再写代码
4. executing-plans — 按计划逐步实施
5. code-review     — 自查 + 修复
```

**铁律：**
- 禁止跳过 TDD — 没有测试的需求不得推进到 IMPLEMENTED
- 禁止跳过 brainstorming — 不假思索的实现 = 返工
- 涉及 A 股数据的，仍走 `a-stock-data` skill

## 需求描述

`auto_experience_health` 任务（2026-09-02）发现经验库质量问题，需经验库重组：

1. **投资经验.md 623 行 > 500 行强制合并线**（必须压缩）：
   - 按章节分组（市场判断/跨市场映射/开盘前评估/数据纪律/早盘纪律设计/策略纪律/宏观数据公布日/超跌反弹/信号设计/板块机会扫描/全市场异动扫描），每个章节合并语义相似条目到 ≤10 条最核心经验。
   - 删除被后续经验覆盖/泛泛而谈/仅对单一交易日有效的条目，删除项移入文件末尾 `<details>` 折叠区备份。

2. **重复章节头合并**：
   - `## 跨市场映射` 出现两次（L83 与 L255）→ 合并为一节
   - `## 超跌反弹` 出现两次（L225 与 L465）→ 合并为一节

3. **重复/高度相似条目去重**（详见 `logs/experience_health_2026-09-02.md` 重复检测节）：
   - P1 主备路径（L329 vs L362，~90%）
   - 跷跷板三阶段 vs 五阶段（L291 vs L497，~80%）
   - 防御资金三要素（L601 vs L621，~85%）
   - 止盈执行跨文件（投资经验 L455 vs 短线机会经验 L199，>95% 完全重复）
   - 换手率筹码松动跨文件（投资经验 L518 vs 短线机会经验 L175，~80-85%）
   - RSI 非抄底信号（投资经验 L300 vs 短线 L29，~70%，建议交叉引用）

4. **矛盾条目标注适用框架**（详见报告矛盾检测节）：
   - 高开默认方向（投资经验 L27 vs 短线 L134）：以"增量资金确认"统一并加交叉引用
   - 晨间>3%不追高 vs 单边日追入（投资经验 L33 vs L415）：加交叉引用
   - 超跌反弹日内常态 vs 熔断日诱多（L13 vs 短线 L13）：显式标注"外围冲击 vs 熔断级"阈值

5. **北向数据断供降级路径标注**：
   - 东财全系北向净买额字段自 2024-08 起断供（返回 NaN/0），实时数据以同花顺 hsgtApi + 本地 CSV 自缓存为替代。
   - 投资经验/短线机会经验中依赖北向实时/前日数据的条目（L269/L273/L150/L27-29/短线 L21-23）统一标注降级路径。

## 期望结果

- 投资经验.md 压缩至 ≤500 行，每章节 ≤10 条核心经验，删除条目入 `<details>` 折叠区备份
- 投资经验.md 无重复章节头、无 >90% 重复条目
- 跨文件重复条目（止盈执行、换手率筹码松动）去重或改为一行引用
- 矛盾条目有显式交叉引用/适用框架标注
- 北向依赖条目标注数据断供降级路径
- 全量回归：`python -m pytest tests/ -v` 通过；现有逻辑巡检（auto_logic_inspect 章节关键词）不回归

## 测试交付物

> 以下清单在 TDD 步骤（auto_req_implement 第 3 步）中填写，在第 3.5 步门禁中验证。

- [ ] 测试文件: `harness/automation/tests/test_experience_health.py`
- [ ] 被测模块: `harness/automation/lib/experience_health.py`（新建：检查文件行数阈值 / 重复章节头 / 必需章节存在性 / 北向断供标注缺失，供健康检查复用）
- [ ] TDD 门禁通过: `python -m pytest harness/automation/tests/test_experience_health.py -v`
- [ ] 回归测试: 全量 `python -m pytest harness/automation/tests/ -v` 通过

**测试覆盖要求：**
- 每个公开函数至少 1 条正向测试 + 1 条边界测试
- 涉及计算的函数需测试正/负/零三种情况
- 涉及状态转换的函数需测试全部合法路径 + 至少 1 条非法路径

## 约束/注意事项

- 经验库内容去重/压缩涉及语义判断，执行时保留最核心、可操作的条目；被删条目必须移入 `<details>` 折叠区备份，不直接删除丢失
- 跨文件去重（投资经验 vs 短线机会经验）以"保留更完整版本 + 另一文件一行引用"为原则，避免丢失上下文
- 矛盾条目不删改语义，仅加显式交叉引用/适用框架标注（类似既有"跷跷板例外条款"模式）
- 北向降级路径标注为文本补充，不改变信号逻辑本身
- 模板/自动化 prompt 涉及章节名的不变（本轮仅经验库文件本身）
- 涉及 A 股数据的，仍走 `a-stock-data` skill

## 处理记录

| 时间 | 操作 | 备注 |
|------|------|------|
| 2026-09-02 | 创建 | auto_experience_health 2026-09-02 发现：投资经验.md 623 行 >500 强制线 + 重复章节头×2 + 6 对重复条目 + 5 对矛盾 + 北向数据源断供未标注；详见 `logs/experience_health_2026-09-02.md` |
| 2026-09-02 | brainstorming | 方案A(最小改动: 仅手工压缩经验md + grep 简单验证) vs 方案B(压缩 + 新建 lib/experience_health.py 纯函数 + tests/test_experience_health.py + tests/test_REQ_005.py 门禁封装)，选择方案B。理由: REQ-005 测试交付物明确要求"被测模块 lib/experience_health.py（供健康检查复用）+ 测试文件 test_experience_health.py"，方案A 无法满足 TDD 门禁；方案B 将行数/重复头/必需章节/北向标注缺失检查固化为可复用纯函数，供 auto_experience_health 复用 |
| 2026-09-02 | writing-plans | 实施计划: 1) experience/投资经验.md — 623→≤500 行压缩，合并重复章节头(跨市场映射×2→1、超跌反弹×2→1)，每章节 ≤10 条核心经验，矛盾条目加交叉引用，北向依赖条目标注断供降级路径，删除/折叠条目入文末 `<details>` 备份；2) experience/短线机会经验.md — 跨文件去重(止盈执行→一行引用、换手率筹码松动→交叉引用、RSI→交叉引用)，矛盾标注(高开方向增量资金统一、外围vs熔断阈值)，北向断供标注；3) lib/experience_health.py — 新建纯函数(count_lines/check_line_limit/find_duplicate_headers/check_required_sections/find_unannotated_northbound_entries/check_experience_health)；4) tests/test_experience_health.py — 新建测试(纯函数单测 + 真实文件集成断言)；5) tests/test_REQ_005.py — TDD 门禁薄封装(复用 test_experience_health.py)；测试策略: `pytest tests/test_experience_health.py -v` 单测 + 全量 `pytest tests/ -v` 回归；回滚: git checkout 经验文件 + 删新增 lib/tests |
| 2026-09-02 | TDD | 测试用例: 37 条 (count_lines 5 / check_line_limit 5 / find_duplicate_headers 6 / check_required_sections 5 / find_unannotated_northbound_entries 6 / check_experience_health 4 / 真实文件集成 6) → 预期: 纯函数全绿；集成测试在压缩前红(623行>500/重复头/北向未标注/章节>10条)、压缩后绿 |
| 2026-09-07 | executing-plans(续) | auto_req_implement 2026-09-07 接管：发现 TDD 门禁 2 集成测试失败(信号设计 11 条/全市场异动扫描 14 条 >10)。完成最后压缩：信号设计 合并"规则1+规则2"→10；全市场异动扫描 合并"ETF异动扫描板块级+互为确认"→1、合并"个股异动扫描业绩驱动/北交所+高开冲高跳水"→1、删除"板块扫描池防御线候选"(与板块机会扫描重复，入备份)、合并"结构性分化日普跌否决+系统性普跌日板块特征"→"真普跌vs结构分化判别"→10；短线机会经验 止盈执行条目改一行引用(完整版保留在投资经验 信号设计)；被删条目入文末 `<details>` 备份 |
| 2026-09-07 | TDD门禁(3.5) | test_REQ_005.py EXISTS + 压缩后重跑 74/74 PASS（此前 2 集成失败全绿）→ 通过 |
| 2026-09-07 | code-review | 自查7项通过；REQ-005 门禁 74/74 PASS + 全量回归 385/385 PASS；投资经验.md 407行≤500、无重复头、必需章节齐全、北向依赖全部标注、每章节≤10条 → 判定 PASS |
| 2026-09-07 | 收尾 | 状态 IN_PROGRESS→IMPLEMENTED；REQ_INDEX.md 同步；task_state.json 更新(req_implement: REQ-005 implemented)；git commit |
| 2026-09-09 | 验证→CLOSED | 测试 test_experience_health.py 37/37 通过；9/9 复盘后投资经验.md 423 行 <500 限制（今日新增 9/9 条目后仍符合）、无重复章节头、必需章节齐全、北向依赖全部标注、每章节 ≤10 条——REQ-005 压缩+去重效果持续有效（9/7 IMPLEMENTED 后连续 2 个交易日验证），闭环完成 |
