# BUG-004: D1 持仓解析失败 — 每日调仓.md 当前持仓表前新增"持仓："说明行致正则失配

- **组件**: prompts/（auto_logic_inspect.md D1 + auto_evening_review.md §10.2）
- **严重程度**: P2(中)
- **违反的正确性定义**: D1 持仓一致性校验 — 每日调仓.md "## 1. 当前持仓" 表应可被机械化解析并与 config/持仓.md 比对
- **状态**: OPEN
- **auto_fix_eligible**: true
  - 判定理由：确定性修复——更新解析正则容忍表前说明行（或统一每日调仓.md 表格格式）
- **发现时间**: 2026-08-26T17:07 CST
- **发现方式**: 代码巡检（auto_logic_inspect D1）
- **来源 REQ**: N/A
- **预期行为**: D1 解析 `## 1. 当前持仓` 下的表格并与 config/持仓.md 比对，输出 PASS（两文件持仓一致）
- **实际行为**:
  - D1 正则 `## 1\.\s*当前持仓\s*\n\s*\n(\|.+\|\s*\n...)` 要求标题后紧接空行+表格；实际文件在标题与表格间新增了"持仓："说明行（`## 1. 当前持仓\n\n\n持仓：\n\n| ...`），正则失配 → `D1:FAIL: 无法解析每日调仓.md的当前持仓表`
  - 人工比对确认：每日调仓.md 与 config/持仓.md 持仓**内容一致**（5 标的 159227/159326/518880/601398/002142，数量成本全同）——非数据不一致，纯解析失败
- **复现步骤**:
  1. 读取 `my_doc/每日复盘/每日调仓.md` "## 1. 当前持仓" 段
  2. 运行 D1 校验脚本（正则要求标题后直接空行+表格）
  3. 因第 10 行"持仓："说明文字存在 → 解析失败
- **受影响文件**:
  - `my_doc/每日复盘/每日调仓.md`（表格前第 10 行"持仓："说明）
  - `my_doc/每日复盘/harness/automation/prompts/auto_logic_inspect.md`（D1 正则）
  - `my_doc/每日复盘/harness/prompts/复盘分析-模板.md`（任务 4.5 同步持仓脚本，使用相同正则）
- **潜在影响**: 持仓一致性校验失效——若未来两文件真出现不一致，D1 无法发现（静默 FAIL 而非具体差异）；auto_evening_review 4.5 持仓同步同样受影响
- **建议修复**: ① 更新解析正则容忍标题与表格间的说明行（如 `## 1\.\s*当前持仓.*?\n\s*\n(\|.+\|\s*\n...)` 加非贪婪或先定位表头行）；② 或删除每日调仓.md 中的"持仓："说明行统一格式；两处脚本（D1 + 复盘模板 4.5）需同步更新

## 测试证据
```
D1:FAIL: 无法解析每日调仓.md的当前持仓表
（文件实际内容：第7行"## 1. 当前持仓"，第10行"持仓："，第12行起为表格）
人工比对：config/持仓.md 与 每日调仓.md 5 标的数量成本完全一致 → 误报，非数据问题
```

## 附注
- BUG-001（WONT_FIX）曾因"每日调仓.md 与 config 不一致"误报，本次是同一解析链路的格式敏感性问题
- 修复后应回归验证：D1 PASS + 复盘模板 4.5 步骤同步校验通过
