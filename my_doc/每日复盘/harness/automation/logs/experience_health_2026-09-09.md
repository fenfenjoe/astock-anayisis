# 经验库健康报告 — 2026-09-09

> 生成时间: 2026-09-09 12:55 (星期三) | 任务: auto_experience_health | 调度: 12:47 周三
> 说明: 记忆体系 Phase 3 后**首次基于 OpenViking 经验库**的健康检查。检查对象为 OpenViking `memories/experiences/`（peer=xiaoman，本会话同 peer），本地 `harness/experience/*.md` 为过渡期镜像（第五(A)步处理）。

## 规模检查

| 领域 | 条目数 | 状态 |
|------|------|------|
| 市场判断 | 1 | OK |
| 开盘前评估 | 1 | OK |
| 数据纪律 | 1 | OK |
| 早盘纪律设计 | 1 | OK |
| 策略纪律 | 1 | OK |
| 超跌反弹 | 1 | OK |
| 信号设计 | 1 | OK |
| 板块机会扫描 | 1 | OK |
| 全市场异动扫描 | 1 | OK |
| 跷跷板套利 | 1 | OK |
| 跨市场套利 | 1 | OK |
| 审阅流程 | 4 | OK |
| 自动化执行规范 | 3 | OK |
| 开发/运维/修复 | 22 | OK |
| **合计** | **40** | **OK（<150 警告线）** |

- 经验库条目总数 40 条（OpenViking `memories/experiences/`），**远低于 150 警告线 / 300 合并线** → 无需触发合并/摘要流程。
- 无 `status=deprecated`/`merged` 条目。

## 重复检测

发现 **3 对** 高度相似条目（均为开发/运维领域，80-95% 相似，需人工确认合并）：

- **dashboard后台多页慢加载优化 vs dashboard后台调度页性能优化 — 相似度 ~80-85%**
  同为 dashboard 云后端性能优化：N+1 查询 + 大字段 `output` 冗余传输 + 连接池复用。后者（调度页）本质是前者的一个具体实例。→ 建议合并（保留更完整的"多页慢加载优化"，调度页条目改为一行引用或并入）。
- **dashboard云端数据库迁移 vs 云迁移后BUG审查修复 — 相似度 ~70-80%**
  重复教训：布尔过滤器大小写（true/false）、SSE 错误静默吞掉、日期选择器失效、信号表格缺列、`select` vs `columns` 参数名。→ 建议交叉引用/合并公共 BUG 教训列表。
- **信号云库化迁移 vs dashboard云端数据库迁移 — 相似度 ~60%**
  共享 PostgREST 批量 upsert "All object keys must match"、云库优先+本地兜底等教训，但范围不同（信号追踪 vs 全站）。→ 建议交叉引用，不强制合并。

未发现 **>95% 完全重复** 条目对 → 无需自动标记 `merged`（第七步自动维护项 1 不触发）。

> 注：投资/短线/审阅领域条目各自独立（每领域 1 条，已在迁移时合并压缩），无内部重复。

## 矛盾检测（贝叶斯裁决）

发现 **0 对** 需贝叶斯裁决的新矛盾条目（无 `status=conflict`/`conflict_with` 元数据条目）。

已确认 **2 对** 既有已知矛盾对（双方在迁移时已内嵌"区分条件/适用框架"，可接受，无需裁决）：

- **跷跷板套利交易规则「系统性 risk-off 日银行正T（单向受益）」 vs A股市场判断投资经验「系统性普跌不做任何方向T」**
  已在跷跷板条目内嵌前置条件：仅"早盘恐慌→全天无修复 + 银行明确收红/领涨"场景适用；全市场普跌（>4000只）且银行仅"少跌"未收红 → 不执行任何方向 T。**适用框架已标注 → 不视为矛盾**。
- **外盘熔断跨市场套利经验「熔断时禁止开盘买入」 vs A股超跌反弹投资经验「次日超跌修复评估」**
  前者限定熔断级恐慌（传导窗口延长至 60 分钟）；后者内嵌"外盘熔断传导日遵守纪律：仓位 1/3 + 10:30 企稳确认 + 回避下跌板块"。**熔断级 vs 外围冲击级边界已标注 → 不视为矛盾**。

无条目需置信度下调/暂存 `conflict`。

## 过时检测

标记 **0 条** 可能过时 / deprecated 清理 **0 条**：

- **元数据缺口（⚠️ 重要发现）**：OpenViking 经验条目**均无 `最近更新` 元数据字段** → 90 天未更新检测无法基于元数据执行（已创建 REQ-009 跟踪）。
- 全部 40 条经验为 2026-09-08（Phase 3 迁移日）沉淀/迁移，无 >90 天未更新条目。
- 市场机制变化核验：T+1 / 涨跌停比例（10%/20cm/30CM）/ 注册制 / 北交所均为现行制度，无变化。
- 数据源变化：北向净买额字段断供（2024-08 起，东财）已在 OpenViking 条目内标注降级路径（A股市场判断投资经验含"北向数据断供时以成交显著放量/两融/盘面承接替代"）✓。
- 引用产品核验：条目引用 ETF/指数（半导体/银行/黄金/有色/农业/创业板等）均为现存产品，无退市/变更。

## 领域标签完整性

- **投资领域: 缺少 [跨市场映射, 宏观数据公布日]** — 本地 `投资经验.md` 有这两章节，但 OpenViking 无对应独立条目
- **短线领域: 缺少 [做T, 事件套利]** — 本地 `短线机会经验.md` 的做T核心纪律（开盘30分钟不操作/系统性普跌不做T/做T胜率校准等）与事件套利未迁移为 OpenViking 独立条目
- **审阅领域: 缺少 [逻辑一致性检查, 完整性检查, 常见错误模式, 每日信号审阅清单]** — 本地 `报告审阅经验.md` 有这些章节，OpenViking 无独立条目

> **⚠️ 领域标签完整性 8 项缺失**（占必需领域近半数）→ 分析前经验注入（小满量化经验记忆系统设计的核心机制）在这些领域失效。已创建 REQ-009 跟踪补全迁移。仅标记，不自动创建（符合第七步约束）。

## 置信度健康

- 高/中/低: **0/40/0**
- 低置信度占比 0% → **OK**（无 >30% 低置信度堆积）
- 高置信度占比 0% **< 20% → ⚠️ 验证性复盘不足**
  - 全部 40 条经验停留"初始置信度为中"，无任何 T2 证据链更新（贝叶斯中→高需 2+ 次独立验证，均未发生）
  - 提示：加强 T2 证据链更新（复盘/周报验证时更新 OpenViking 对应条目置信度），而非仅本地文件
  - 已创建 REQ-009 跟踪置信度更新循环落地

## 镜像导出（过渡期）

- **已备份 .bak（3 份）**：`投资经验.md.bak` / `短线机会经验.md.bak` / `报告审阅经验.md.bak` ✓
- **本次未执行 OpenViking→本地 覆写导出**，原因（重要）：
  - OpenViking 条目为 09-08 迁移的**浓缩版**（Situation/Approach/Reflect 摘要），本地镜像文件更丰富（案例、交叉引用、`<details>` 备份区），且 OpenViking 缺 `跨市场映射`/`宏观数据公布日`/`做T`/`事件套利` 等章节
  - 若按 prompt 第五(A)步直接 OpenViking→本地 覆盖，将**丢失这些章节与大量细节**，违反"导出后校验非空且章节完整"（导出结果将缺章节）
  - **方向修正建议**：镜像导出方向改为 **本地→OpenViking 补齐**（先补全 OpenViking 侧 8 个缺失领域 + 元数据，再恢复双向同步）→ 已创建 REQ-009
- 本地镜像健康核验（REQ-005 门禁）：
  - 投资经验.md：423 行（≤500）✓ 无重复章节头 ✓ 必需章节齐全 ✓ 北向标注 ✓；章节条目数：市场判断 12（≤20 镜像边界 ✓）、其余章节 ≤10
  - 短线机会经验.md：231 行，无重复头；北向软标注观察项（L13/87/103/140 提及"北向断供/流出"但无"北向数据断供降级"精确标记，属既有软标注，非本次新增）
  - 报告审阅经验.md：146 行，无重复头

## Steering 系统健康

- 僵死 REQ (OPEN>7天): **0** — REQ-006(OPEN 6天)、REQ-008(OPEN 1天)、etf REQ-001(OPEN 6天) 均未超 7 天
- 滞留 REQ (IN_PROGRESS>3天): **0** — 无 IN_PROGRESS
- 卡住 REQ (IMPLEMENTED>5天): **1 — REQ-003**（IMPLEMENTED 2026-09-01，8 天）
  - ⚠️ 但为**有意观察期**：REQ-003 等待 10/17 三季报窗口验证"业绩驱动类别"纳入率，处理记录明确"保持 IMPLEMENTED 待 10/17"。建议明确收尾时间（10/17 后若仍 OPEN 需人工裁决 CLOSED）
  - REQ-005（IMPLEMENTED 09-07，2 天）未卡住
- 无测试 IMPLEMENTED (>3天): **0** — REQ-003 ✓ `test_REQ_003.py`、REQ-005 ✓ `test_REQ_005.py`（detect_stuck 校验通过：OK: All IMPLEMENTED REQs have test files）
- 积压程度: **3 OPEN**（每日复盘 2 + etf 1）— OK（<5）
- 30日闭环率 (08-10~09-09): **50%** (2 CLOSED / 4 处理) — ⚠️ 需关注
  - 创建总数 6：REQ-003(08-28)、REQ-004(09-01)、REQ-005(09-02)、REQ-006(09-03)、REQ-007(09-07)、REQ-008(09-08)
  - 仍 OPEN 2（REQ-006/008）；IMPLEMENTED 2（REQ-003/005）；CLOSED 2（REQ-004/007）
  - 注：REQ-003 卡在观察期、REQ-005 刚 IMPLEMENTED 2 天，收尾后闭环率将上升
- 目录完整性: **基本正常（已修复 2 项 + 1 项待人工）**
  - ✅ 已修复：REQ-004（CLOSED）、REQ-007（CLOSED）文件从 `open/` 移入 `closed/`（对齐 REQ-001/002 约定）
  - ⚠️ 待人工：**etf-strategies REQ-002（INDEX: IMPLEMENTED）无对应文件 → ORPHAN**（open/closed/in_progress 均无）。建议：补建 REQ-002.md 文件（INDEX 内已有详细实施记录可作内容基础）或从 INDEX 标注"文件缺失待补"
  - 交叉校验通过：每日复盘 9 个 REQ 文件齐全无 UNREGISTERED；etf REQ-001 文件存在

## 综合评估

**需要关注**

- OpenViking 经验库迁移不完整：8 个必需领域缺独立条目（跨市场映射/宏观数据公布日/做T/事件套利/审阅 4 项）→ 领域注入与检索失效
- Schema 元数据（领域/状态/置信度/最近更新）全部缺失 → 过时检测与置信度健康无法结构化执行
- 置信度全部停滞"初始中"，高置信度占比 0%（<20% 警戒）→ 贝叶斯 T2 证据链更新未在 OpenViking 落地
- 镜像导出方向需修正（OpenViking 浓缩版 vs 本地详细版，直接覆盖会丢数据）
- Steering 系统基本健康：0 僵死 / 0 滞留 / 0 无测试，目录已整理；仅 30 日闭环率 50%（有观察期原因）与 etf REQ-002 ORPHAN 需关注

## 建议操作

1. **【必须·架构性改动→REQ-009 已创建】** OpenViking 经验库补全迁移：补建 8 个缺失领域条目（从本地镜像迁移）+ 全部条目补齐 Schema 元数据（领域/状态/置信度/首次沉淀/最近更新）+ T2 置信度更新循环落地（复盘/周报联动）+ 镜像导出方向修正（本地→OpenViking 补齐）。由 `auto_req_implement` 走 superpowers 流程实施。
2. **【建议·低风险·本次已执行】** REQ-004/REQ-007（CLOSED）文件移入 `closed/` 保持目录一致。
3. **【建议·需人工】** etf-strategies REQ-002（IMPLEMENTED）无文件 → 补建 REQ-002.md 或标注说明。
4. **【建议·需人工】** REQ-003 观察期收尾时间明确：10/17 三季报窗口后复核，验证完成即 CLOSED。
5. **【建议】** dashboard 后台性能优化两条目（多页/调度页，~80-85%）与云迁移 BUG 教训条目（迁移/审查，~70-80%）在下次开发任务中合并/交叉引用。
6. **【建议】** 后续 `auto_experience_health` 在 OpenViking 侧补全元数据后，恢复过时检测（最近更新>90天）与置信度健康的结构化统计。
