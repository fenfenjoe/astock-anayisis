# BUG-029: pet.js 引用已移除的桌宠场景层挂载点 `xm-pet-scene`，dashboard.html 无该元素 — 前端契约断裂

- **组件**: dashboard
- **严重程度**: MEDIUM
- **违反的正确性定义**: 不适用（DASH-001~003 未覆盖桌宠 DOM 契约；属前端静态契约不一致）
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：修复方向二选一需人工决策 — (a) 按 pet.js 注释「场景层：已移除」清理死引用（删 `el.scene` + 测试 PET_IDS 条目） vs (b) 恢复 `xm-pet-scene` 挂载点到 HTML。属前端契约定义决策，按「不确定时默认 false（安全原则）」标记人工
- **发现时间**: 2026-09-08T12:25+08:00
- **发现方式**: 每日代码巡检 (bug_inspect_code) 第四步全量测试
- **预期行为**: `test_pet_frontend.py::test_pet_ids_in_html`（PET_IDS 含 `xm-pet-scene`）与 `test_pet_js_id_refs_exist_in_html`（pet.js `$('xm-pet-scene')` 引用）均通过 — 即 pet.js 引用的每个元素 id 都存在于 dashboard.html 模板
- **实际行为**: 2 项测试 FAILED：
  - `dashboard.html`（templates/dashboard.html:506-524）桌宠结构仅含 `xm-pet / xm-pet-stage / xm-pet-canvas / xm-pet-fallback / xm-pet-bubble / xm-pet-zzz / xm-pet-modes / xm-pet-btn-min / xm-pet-btn-hide / xm-pet-mini`，**没有 `xm-pet-scene`**；
  - `pet.js:134` 仍执行 `el.scene = $('xm-pet-scene')`，拿到 `null`（死引用，`el.scene` 声明于 line 126、赋值后从未被读取；`buildScene()`（line 141）为空函数 — 场景层已在 pet.js 注释（line 10「场景层：已移除」）声明移除，但 JS 引用与测试契约未同步清理；
  - 全量/单跑均稳定 FAILED（纯文件内容契约检查，与运行顺序无关）。
- **复现步骤**:
  1. `cd etf-strategies && python -m pytest tests/test_pet_frontend.py::test_pet_ids_in_html tests/test_pet_frontend.py::test_pet_js_id_refs_exist_in_html -v` → 2 FAILED（`dashboard.html 缺挂载点 #xm-pet-scene` / `pet.js 引用但模板缺失的 id: ['xm-pet-scene']`）
  2. `grep -n "xm-pet-scene" dashboard/templates/dashboard.html` → 无输出（确认模板缺元素）；`grep -n "xm-pet-scene" dashboard/static/js/pet.js` → line 134（确认 JS 引用）
- **受影响文件**:
  - `etf-strategies/dashboard/static/js/pet.js:126,134`（`el.scene` 声明 + 死引用）
  - `etf-strategies/dashboard/templates/dashboard.html:506-524`（桌宠挂载区，缺 `xm-pet-scene`）
  - `etf-strategies/tests/test_pet_frontend.py:24-28,49-63`（PET_IDS 契约 + 2 项断言）
- **潜在影响**: 仅前端桌宠（看板娘）功能 — `el.scene` 为 null 但未被使用，运行时无崩溃；影响为测试门禁红色（2 项 FAILED）+ 契约混乱（后续若恢复场景层需重加挂载点）。对回测/信号/数据无影响。
- **建议修复**: 二选一：
  (a) 确认场景层永久移除 → 删除 `pet.js` 的 `el.scene` 声明/赋值与 `tests/test_pet_frontend.py` PET_IDS 中的 `xm-pet-scene`，保持测试与实现一致；
  (b) 若仍需场景层 → 在 `dashboard.html` 桌宠 stage 内恢复 `<div class="xm-pet-scene" id="xm-pet-scene"></div>` 挂载点。
  推荐 (a)（与 pet.js 注释「场景层：已移除」一致，消除死代码）。

## 巡检证据
```
$ python -m pytest tests/test_pet_frontend.py -v --tb=line
FAILED tests/test_pet_frontend.py::test_pet_ids_in_html - AssertionError: dashboard.html 缺挂载点 #xm-pet-scene
  assert 'id="xm-pet-scene"' in '...dashboard.html 全文...'
FAILED tests/test_pet_frontend.py::test_pet_js_id_refs_exist_in_html - AssertionError: pet.js 引用但模板缺失的 id: ['xm-pet-scene']
  assert not ['xm-pet-scene']
=================== 3 failed, 3 passed, 1 warning in 0.76s ====================

$ grep -n "xm-pet-scene" dashboard/static/js/pet.js
134:     el.scene = $('xm-pet-scene');

$ grep -n "xm-pet-scene" dashboard/templates/dashboard.html
（无输出）
```

## 自动修复判定
- **判定时间**: 2026-09-08T14:09+08:00
- **判定结果**: 不自动修复 — 前端契约定义决策（删除死引用 vs 恢复挂载点），修复方向需人工裁决
- **建议**: 人工审阅后执行 bug_fix_template.md 流程（推荐方向 (a)：确认场景层永久移除后清理 pet.js 死引用与测试契约）
