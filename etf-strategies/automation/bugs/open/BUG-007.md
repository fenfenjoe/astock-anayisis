# BUG-007: ENG-004 换手率定义与实现口径不一致 — 引擎返回年化换手率(可>2)，定义描述单次调仓(≤2)

- **组件**: engine
- **严重程度**: MEDIUM
- **违反的正确性定义**: ENG-004（换手率计算正确：turnover = sum(abs(weight_diff)) 每次调仓；verification: turnover >= 0, turnover <= 2 全仓切换=2）
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：涉及计算公式口径（ENG-004），"年化换手率"还是"单次调仓换手率"属指标语义决策，需人工判断，不机械修复（同 BUG-005 MET-005 口径决策模式）
- **发现时间**: 2026-08-28T12:22 CST
- **发现方式**: 每日代码巡检
- **预期行为**: 按 correctness_definitions.yaml ENG-004，引擎返回的 `turnover` 应为单次调仓换手率 `sum(abs(weight_diff))`，全仓切换时 ≤ 2
- **实际行为**: `backtest/engine.py` L43-45 计算**年化换手率**：
  ```python
  total_turnover = weight_change.sum().sum()
  years = len(prices) / 252
  ann_turnover = total_turnover / years if years > 0 else 0.0
  ```
  月度全仓切换策略实测 `turnover = 23.50`（远超定义 verification 的 `≤ 2` 上限）。定义描述的口径是"每次调仓的权重变动绝对值之和"（单次全仓切换=2），而实现返回的是"全期累计换手 ÷ 年数"的年化值
- **复现步骤**:
  1. `cd etf-strategies && python -c "import pandas as pd; from backtest.engine import backtest; from backtest.cost import Cost; dates=pd.bdate_range('2024-01-01', periods=504); prices=pd.DataFrame({'A':100.0,'B':100.0}, index=dates); w=pd.DataFrame({'A':0.0,'B':1.0}, index=dates); [w.loc[dt].update({'A':1.0,'B':0.0}) if (i//21)%2==0 else w.loc[dt].update({'A':0.0,'B':1.0}) for i,dt in enumerate(dates)]; r=backtest(prices,w,cost=Cost()); print('turnover =', r.turnover)"` → 输出 `turnover = 23.50`
  2. 按 ENG-004 verification，全仓切换单次换手应为 2，年化口径下可远超 2
- **受影响文件**:
  - `etf-strategies/backtest/engine.py:43-45`（年化换手率计算）
  - `etf-strategies/automation/config/correctness_definitions.yaml:30-34`（ENG-004 定义口径与实现不符）
  - `etf-strategies/tests/test_engine.py`（ENG-004 的 test 字段指向文件级，无换手率语义专项测试；grep turnover 仅注释提及）
- **潜在影响**: 正确性定义 ENG-004 的 verification（turnover ≤ 2）与实现矛盾，导致定义无法作为回归校验基准；若未来有人按定义断言 turnover ≤ 2 将误报失败。年化换手率本身是合理指标（dashboard/报告均展示"年化换手"），问题在于定义未同步年化口径
- **建议修复**: 人工确认指标口径后二选一：(a) 若保留年化换手率（推荐，与 dashboard/html_report 一致），更新 correctness_definitions.yaml ENG-004 为"年化换手率 = 全期累计权重变动÷年数"，并补 `tests/test_engine.py` 换手率专项测试；(b) 若坚持单次调仓口径，需引擎按调仓事件输出，影响面大

## 巡检证据
```
$ python _verify_eng004.py
engine turnover (annualized): 23.50
定义 verification: turnover <= 2 (全仓切换=2)

$ grep -n "turnover\|换手" tests/test_engine.py
    """再平衡算术：价格不变时仅首日建仓扣成本，之后无换手"""   # 仅注释，无断言

$ python -m pytest tests/ -q --cov=backtest --cov-report=term | tail -5
TOTAL                                              1397    225    84%
361 passed, 2 skipped in 85.06s
```

## 自动修复判定
- **判定时间**: 2026-08-28T14:07+08:00
- **判定结果**: 不自动修复 — 涉及计算逻辑/算法/金融公式（ENG-004 换手率指标口径：年化 vs 单次调仓，属指标语义决策）
- **建议**: 人工审阅后执行 bug_fix_template.md 流程（建议方向 (a)：保留年化换手率，同步更新 correctness_definitions.yaml ENG-004 定义并补专项测试）
