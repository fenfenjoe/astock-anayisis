# BUG-014: S12/S13 KB 因子口径描述与实现不符 — 「年化收益」实为简单区间收益；「成交量因子」实为价格波幅代理

- **组件**: strategies (S12 SentimentMomentum / S13 MultiFactor)
- **严重程度**: LOW
- **违反的正确性定义**: 逻辑巡检 2.1「KB 描述 vs 代码实现对齐」（S12/S13 行）
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：因子口径属策略定义/文档问题，需人工确认以代码为准还是以 KB 为准
- **发现时间**: 2026-08-31T12:45 CST
- **发现方式**: 逻辑巡检 2.1 逐策略对齐检查（S12/S13）
- **预期行为**: KB 描述与实现一致：S12「① 动量因子(30%)：20日年化收益」「② 量价情绪因子(25%)：量价配合度+成交量趋势」「④ 量价关系因子(25%)：放量下跌/放量上涨」；S13「① 动量因子(40%)：60日年化收益」「④ 成交量因子(15%)：量价关系/资金关注度」，且 KB weaknesses 提到「成交量数据质量依赖东财API」（暗示真实成交量参与计算）
- **实际行为**: 两处口径不符：
  1. **年化 vs 简单收益**：S12 `sentiment_momentum.py:54` 与 S13 `multi_factor.py:61` 均为 `mom = closes[-1] / closes[0] - 1.0`（区间简单收益），非「年化收益」。年化为单调变换，排名不受影响，但分数绝对值与 KB 表述不符
  2. **成交量 vs 价格波幅代理**：S12 F2/F4（L58-62、L71）与 S13 F4（L80-91）均用价格日波动幅度（`np.abs(np.diff(closes))/closes[:-1]` 或 `np.std(np.diff(closes))`）代理成交量，无真实成交量数据参与（回测入口 `run_backtest.py` 只加载 `df["close"]`，策略层拿不到 vol 列）。代码注释明示「无真实volume列时」「用价格波动幅度代理」，属实现变通，但 KB 未同步说明
- **复现步骤**:
  1. 读取 `backtest/strategies/sentiment_momentum.py` L53-62/L71 与 `backtest/strategies/multi_factor.py` L60-68/L80-91，观察动量因子公式与成交量代理实现
  2. 对比 `strategy_kb.py` S12 factors 与 S13 factors 的「年化收益」「成交量」表述
- **受影响文件**:
  - `etf-strategies/backtest/strategies/sentiment_momentum.py:54,58-62,71`
  - `etf-strategies/backtest/strategies/multi_factor.py:61,80-91`
  - `etf-strategies/strategy_kb.py` S12 factors / S13 factors
- **潜在影响**: 极低~低。排名行为不受年化差异影响；成交量代理使「量价」因子的信息含量低于 KB 描述（价格波幅≠真实成交活跃度），S12/S13 的实际因子构成与设计文档有出入，影响策略可解释性与后续优化决策（如接入真实成交量列后的行为变化不可预期）
- **建议修复**: 更新 KB S12/S13 因子描述：将「年化收益」改为「区间收益」，将「成交量因子」明确标注为「价格波幅代理（当前无真实成交量输入，回测仅加载收盘价）」，或在回测/信号链路接入真实 vol 列后回填 KB

## 巡检证据
```
# S12 sentiment_momentum.py
L54: mom = closes[-1] / closes[0] - 1.0                     # 20日简单收益, 非年化
L58-62: daily_range = np.abs(np.diff(closes)) / closes[:-1] # 价格波幅代理成交量
L71:    pv_corr = mom * vol_sentiment                        # 量价协同(代理)

# S13 multi_factor.py
L61: mom = closes[-1] / closes[0] - 1.0                     # 60日简单收益, 非年化
L80-91: recent_vol/hist_vol = np.std(np.diff(closes...))    # 价格波幅代理成交量

# run_backtest.py L38: series[code] = df["close"]           # 只加载收盘价, vol 列不进入策略

# KB S12: "① 动量因子(30%)：20日年化收益" "② 量价情绪因子(25%)：量价配合度+成交量趋势"
# KB S13: "① 动量因子(40%)：60日年化收益" "④ 成交量因子(15%)：量价关系"
```

## 自动修复判定
- **判定时间**: 2026-08-31T14:15+08:00
- **判定结果**: 不自动修复 — 涉及计算逻辑/算法/金融公式（因子口径以代码为准还是以 KB 为准需人工确认）
- **建议**: 人工审阅后执行 bug_fix_template.md 流程
