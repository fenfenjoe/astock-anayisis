# BUG-030: pet.css 桌宠状态/姿态样式覆盖不完整 — 缺 `data-state="working"` 与全部 `data-posture` 规则

- **组件**: dashboard
- **严重程度**: MEDIUM
- **违反的正确性定义**: 不适用（DASH-001~003 未覆盖桌宠视觉契约；属前端静态契约不一致）
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：补 `data-state="working"` 与 `data-posture=*` 的 CSS 规则属于**视觉设计决策**（working 态应呈现什么效果、姿态是否需要区分），非机械确定性修复；且 `pet.js` 虽声明 `state.posture` 却从不写入 DOM `data-posture` 属性，修复需同时决策 JS 侧是否落属性。按「不确定时默认 false（安全原则）」标记人工
- **发现时间**: 2026-09-08T12:25+08:00
- **发现方式**: 每日代码巡检 (bug_inspect_code) 第四步全量测试
- **预期行为**: `test_pet_frontend.py::test_pet_css_covers_states_and_postures` 要求 pet.css 覆盖 pet.js 会设置的全部视觉态：
  - `data-state`：offline / leave / sleep / slack / working / thinking（unknown=初始默认态豁免）
  - `data-posture`：type / code / data / read
- **实际行为**: 测试 FAILED（`pet.css 缺 data-state=working 样式`）：
  - `pet.css`（59-78 行）仅有 `offline / leave / sleep / slack / thinking` 5 个 `data-state` 规则，**缺 `working`**（working 是最常见的工作态，由 pet.js:263 `el.pet.setAttribute('data-state', key)` 高频设置，CSS 无专属规则 → 永远走默认无滤镜）；
  - `pet.css` 全文 **0 条 `data-posture` 规则**（`grep data-posture` 仅命中头部注释行 9 的 data-state 说明，无双引号属性选择器）— 而测试要求 type/code/data/read 4 个姿态全覆盖；
  - 潜在根因：`pet.js` 声明 `posture: 'type'`（line 104）并在 `stateLine()`（line 305）使用，但**从未** `setAttribute('data-posture', ...)`、也从未更新 `state.posture` 值 → `data-posture` 机制整体处于半成品状态（JS 不落属性 + CSS 无规则 + 测试要求两者）。
- **复现步骤**:
  1. `cd etf-strategies && python -m pytest tests/test_pet_frontend.py::test_pet_css_covers_states_and_postures -v --tb=line` → FAILED（`pet.css 缺 data-state=working 样式`）
  2. `grep -n 'data-state="working"' dashboard/static/css/pet.css` → 无输出；`grep -n 'data-posture' dashboard/static/css/pet.css` → 仅 line 9 注释
  3. `grep -n "setAttribute('data-posture'" dashboard/static/js/pet.js` → 无输出（JS 从未写 data-posture）
- **受影响文件**:
  - `etf-strategies/dashboard/static/css/pet.css:9,59-78`（状态规则缺 working、姿态规则全缺）
  - `etf-strategies/dashboard/static/js/pet.js:104,305`（`state.posture` 声明/使用但从不落 DOM 属性）
  - `etf-strategies/tests/test_pet_frontend.py:29-30,66-75`（STATES/POSTURES 契约 + 断言）
- **潜在影响**: 桌宠 working 态与其余 5 态视觉无差异（缺专属滤镜/动效），`data-posture` 姿态细分功能不生效（JS 不写属性 → CSS 即使有规则也不触发）；测试门禁红色（1 项 FAILED，且修复 working 后还会暴露 posture 断言失败）。对回测/信号/数据无影响。
- **建议修复**: 
  1. `pet.css` 补 `data-state="working"` 规则（如工作态清晰微亮 `filter: saturate(1.05)`，或显式 identity 规则满足契约）；
  2. 决策 `data-posture` 机制去留：若保留 → `pet.js` 在 `paint()` 中同步 `setAttribute('data-posture', state.posture)` 并补全 4 条 CSS 规则；若废弃 → 从测试 POSTURES 与 pet.js 中同步移除。推荐先补 working 规则 + 让 posture 机制闭环（JS 落属性 + CSS 规则）。

## 巡检证据
```
$ python -m pytest tests/test_pet_frontend.py::test_pet_css_covers_states_and_postures -v --tb=line
FAILED tests/test_pet_frontend.py::test_pet_css_covers_states_and_postures - AssertionError: pet.css 缺 data-state=working 样式
  assert 'data-state="working"' in '/* ...pet.css 全文... */'

$ grep -n 'data-state=' dashboard/static/css/pet.css
59: .xm-pet[data-state="offline"] .xm-pet-canvas,
63: .xm-pet[data-state="leave"] .xm-pet-canvas,
67: .xm-pet[data-state="sleep"] .xm-pet-canvas,
72: .xm-pet[data-state="slack"] .xm-pet-canvas,
77: .xm-pet[data-state="thinking"] .xm-pet-canvas,
（无 working；无任何 data-posture）

$ grep -n "setAttribute" dashboard/static/js/pet.js
263:     el.pet.setAttribute('data-state', key);
620:     el.pet.setAttribute('data-bound', '1');
746:     el.pet.setAttribute('aria-hidden', 'false');
764:     if (el.pet) { el.pet.classList.add('is-min'); el.pet.setAttribute('aria-hidden', 'true'); }
（无 data-posture）
```

## 自动修复判定
- **判定时间**: 2026-09-08T14:09+08:00
- **判定结果**: 不自动修复 — 视觉设计决策 + posture 机制去留需人工裁决
- **建议**: 人工审阅后执行 bug_fix_template.md 流程（推荐先补 data-state=working 规则 + 让 posture 机制闭环或整体移除）
