# BUG-003: 动量家族 R² 截断不一致 — S4/S8 未用 max(r_sq, 0)，其余(14/15/16/17/18/19)已截断

- **组件**: strategies
- **严重程度**: LOW
- **违反的正确性定义**: 逻辑巡检第三步 3.1「共享公式一致性」— 所有使用「年化收益 × R²」公式的策略计算方式应完全一致
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: true
  - ⚠️ 注：虽标记 auto_fix_eligible=true，但修复涉及修改动量打分公式（`ann_ret * r_sq` → `ann_ret * max(r_sq, 0)`），属**信号生成/金融计算公式**范畴。按 bug_auto_fix 安全约束「绝不修改金融计算公式」，自动修复进程**未实施**修复，升级人工审核。
  - 判定理由：机械性一致性问题，且代码库已有明确标准佐证（`dashboard/app.py:1111,1240` 注释明示 `clamp_r2=True 用于 S15/S16/S17，与策略内部的 max(r_sq, 0) 一致`；`S15 使用 max(r_sq, 0) 截断 R²`），属于「有明确标准佐证的机械性修正」
- **发现时间**: 2026-08-10 (bug_inspect_logic 周度逻辑巡检)
- **发现方式**: 逻辑巡检 3.1 共享公式一致性检查
- **预期行为**: 家族内所有「年化收益 × R²」打分对 R² 的处理一致。`dashboard/app.py` 已把 `max(r_sq, 0)` 定义为该公式族的标准写法（clamp_r2=True 服务于 S15/S16/S17 并注明「与策略内部一致」），因此 S4/S8 也应使用 `max(r_sq, 0)` 截断，避免负 R² 翻转得分符号
- **实际行为**: 同一公式族存在两种 R² 处理：
  - **截断**（`max(r_sq, 0)`）：S14 `adaptive_momentum.py:55`、S15 `rsrs_momentum.py:64`、S16/S17 `canary_defense.py:72`、S18 `rsrs_reversal_momentum.py:75`、S19 `low_correlation_rotation.py:58`
  - **不截断**（`ann_ret * r_sq` 原始值）：S4 `momentum_rotation.py:63`、S8 `three_factor_momentum.py:52`
  - 当某 25 日窗口的 OLS 拟合差于水平均值线时（ss_res > ss_tot），R² 可轻微为负；此时即便年化收益为正，S4/S8 的得分也会被翻转为负，与家族其他策略的判定不一致
- **复现步骤**:
  1. 对比 7 个策略的 `_momentum_score` / `_slope_factor` / `scores` 公式：
     - `grep -n "r_sq\|r_squared\|max(r_sq" backtest/strategies/momentum_rotation.py backtest/strategies/three_factor_momentum.py backtest/strategies/adaptive_momentum.py backtest/strategies/rsrs_momentum.py backtest/strategies/canary_defense.py backtest/strategies/rsrs_reversal_momentum.py backtest/strategies/low_correlation_rotation.py`
  2. 观察 S4 (`momentum_rotation.py:63`) 与 S8 (`three_factor_momentum.py:52`) 直接 `return ann_ret * r_sq`，未做 `max(r_sq, 0)`
- **受影响文件**:
  - `etf-strategies/backtest/strategies/momentum_rotation.py`（S4，第 63 行）
  - `etf-strategies/backtest/strategies/three_factor_momentum.py`（S8，第 52 行）
- **潜在影响**: 极低。25 日 log-price 窗口的 R² 通常 >0，仅在高波动/非单调窗口可能轻微为负；且 S4/S8 回测收益仍高达 34.46%/34.29%，未表现出异常。此 BUG 属代码一致性/可维护性问题，非功能性错误，不影响当前回测结果判定
- **建议修复**: 将 S4/S8 的得分改为 `ann_ret * max(r_sq, 0)`（或 `max(r_sq, 0.0)`），与家族其他 5 个策略及 `dashboard/app.py` 的 clamp_r2 标准对齐。修改后重跑 `run_backtest.py` 确认 S4/S8 指标无显著变化

## 巡检证据
```
# dashboard/app.py 已明确的公式族标准（佐证 clamp 为预期行为）
app.py:1111  clamp_r2=True 用于 S15/S16/S17，与策略内部的 max(r_sq, 0) 一致
app.py:1240  S15 使用 max(r_sq, 0) 截断 R²，与 TrendFilterMomentum._momentum_score 一致

# 家族内 R² 处理对比
S4  momentum_rotation.py:63        return annualized_return * r_squared        # 不截断
S8  three_factor_momentum.py:52    return ann_ret * r_sq                       # 不截断
S14 adaptive_momentum.py:55        return ann_ret * max(r_sq, 0)               # 截断
S15 rsrs_momentum.py:64            return ann_ret * max(r_sq, 0)               # 截断
S16 canary_defense.py:72           return ann_ret * max(r_sq, 0)               # 截断
S18 rsrs_reversal_momentum.py:75   return ann_ret * max(r_sq, 0.0)             # 截断
S19 low_correlation_rotation.py:58 return ann_ret * max(r_sq, 0.0)             # 截断
```

## 自动修复判定
- **判定时间**: 2026-08-10T13:14 CST
- **判定结果**: 不自动修复 — 修复需修改动量打分公式（信号生成/金融公式），超出 bug_auto_fix 允许修改范围（注册/导入/路径/配置/测试/文档）
- **建议**: 人工审阅后执行 bug_fix_template.md 流程，将 S4 `momentum_rotation.py:63` 与 S8 `three_factor_momentum.py:52` 改为 `ann_ret * max(r_sq, 0)`，与家族其他策略对齐
