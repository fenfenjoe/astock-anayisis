# BUG-013: S12 KB 描述「放量下跌触发避险信号时自动切换至货币ETF」未实现 — 代码仅做 0.3 分惩罚，放量下跌日仍满仓持有

- **组件**: strategies (S12 SentimentMomentum)
- **严重程度**: MEDIUM
- **违反的正确性定义**: 逻辑巡检 2.1「KB 描述 vs 代码实现对齐」（S12 行）
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：涉及避险信号触发逻辑（策略行为），是否改为硬切换属设计决策，需人工确认
- **发现时间**: 2026-08-31T12:45 CST
- **发现方式**: 逻辑巡检 2.1 逐策略对齐检查（S12）+ 场景实证
- **预期行为**: KB S12 三处明确描述「放量下跌触发避险信号时自动切换至货币ETF」「放量下跌（成交量放大+价格下跌）触发避险信号，自动空仓持货币」「放量下跌信号触发时当日即切换至货币（511880）」——即放量下跌日应强制空仓持货币 511880
- **实际行为**: `sentiment_momentum.py` L80-81 仅执行 `score -= 0.3`（放量下跌扣分惩罚），无任何强制切换货币逻辑。切货币的唯一条件是 L90 `if best_score <= 0`（最高综合得分≤0）。实测构造全部 4 只 ETF 下跌+高波动（放量下跌代理）场景，S12 最后一日仍满仓 513100（货币权重 0.000），避险机制未触发
- **复现步骤**:
  1. 读取 `backtest/strategies/sentiment_momentum.py` L80-91：惩罚扣分 + `best_score <= 0` 判断，无放量下跌硬切换分支
  2. 构造全标的下跌+高波动价格序列（`100*(0.99**i) + 3*sin(i/3)`）喂入 `SentimentMomentum(lookback=20, top_n=1).generate()`，检查最后一日 `511880` 权重
- **受影响文件**:
  - `etf-strategies/backtest/strategies/sentiment_momentum.py:80-91`
- **潜在影响**: KB 所述「放量下跌避险」在恐慌市（如 2015/2018/2024 急跌放量段）不会主动空仓，策略在下跌放量期仍保持满仓（或仅轻微降分），KB 卖点「量价关系提供传统动量策略缺少的风险预警」名不副实；S12 回测最大回撤 -30.59% 与 KB 描述的保护作用不完全吻合
- **建议修复**: 人工裁决：(A) 若设计意图是硬避险 → 在放量下跌条件（`mom < -0.05 and vol_sentiment > 0.5`）命中时直接置 `weights[i, cash] = 1.0`（或大幅降仓），与 KB 对齐；(B) 若接受软惩罚 → 更新 KB 描述为「放量下跌显著扣分，非强制空仓」。修改后重跑 `run_backtest.py` 对比 S12 指标

## 巡检证据
```
$ python -c "S12 放量下跌场景实证（4只ETF全部下跌+高波动, lookback=20, top_n=1）"
最后5日权重(513100,159915,510180,518880,511880):
2024-03-18: 513100=1.0 其余=0.0
...
最后一日货币权重 = 0.000 (1.0=已避险, 0.0=仍持仓)   ← 避险未触发

# 源码关键段 (sentiment_momentum.py L80-91)
if mom < -0.05 and vol_sentiment > 0.5:
    score -= 0.3  # 放量下跌 = 危险信号（仅扣分）
...
if best_score <= 0:                      # 唯一切货币条件
    weights.iloc[i, weights.columns.get_loc(self.cash)] = 1.0

# KB S12: "放量下跌触发避险信号时自动切换至货币ETF"
#         "放量下跌信号触发时当日即切换至货币（511880）"
```

## 自动修复判定
- **判定时间**: 2026-08-31T14:15+08:00
- **判定结果**: 不自动修复 — 涉及计算逻辑/算法/金融公式（避险信号是否硬切换属策略行为设计决策）
- **建议**: 人工审阅后执行 bug_fix_template.md 流程
