# BUG-012: S8 三因子未归一化即加权，效率因子支配综合得分（实测占84.3%），与 KB「三因子归一化后加权(0.4/0.3/0.3)」严重不符

- **组件**: strategies (S8 ThreeFactorMomentum)
- **严重程度**: MEDIUM
- **违反的正确性定义**: 逻辑巡检 2.1「KB 描述 vs 代码实现对齐」（S8 行）；逻辑巡检 3.1「共享公式一致性」
- **状态**: MANUAL_REVIEW
- **auto_fix_eligible**: false
  - 判定理由：涉及策略打分公式（金融算法），修改将改变 S8 信号行为，必须人工确认后决定修复方案（修复方向二选一：加归一化 / 更新 KB 描述）
- **发现时间**: 2026-08-31T12:45 CST
- **发现方式**: 逻辑巡检 2.1 逐策略对齐检查（S8）+ 因子量级实测
- **预期行为**: KB 描述「三因子归一化后加权」（斜率40% + 乖离30% + 效率30%）——三个因子在可比量级下按 0.4/0.3/0.3 权重合成综合得分
- **实际行为**: `three_factor_momentum.py` 的 `generate()` 直接对三个原始因子做加权求和 `score = 0.4*f1 + 0.3*f2 + 0.3*f3`，未做任何归一化/标准化。由于 F3（效率动量）自带 `×100` 放大（`_efficiency_factor` 返回 `momentum * efficiency_ratio * 100`），其原始量级远大于 F1/F2，实际对综合得分起支配作用。实测（标准测试夹具 `_prices_multi`，513100 最后 25 日窗口）：
  - F1 斜率动量 = 0.6479（加权贡献 0.259）
  - F2 乖离动量 = 0.0324（加权贡献 0.010）
  - F3 效率动量 = 4.7952（加权贡献 1.439，**占综合得分 84.3%**）
  - 代码注释（L121-126）声称「这里用简单的标准化」，但实际未执行任何标准化步骤，注释与实现自相矛盾
- **复现步骤**:
  1. 读取 `backtest/strategies/three_factor_momentum.py` L121-127：`score = (self.w_slope * f1 + self.w_bias * f2 + self.w_efficiency * f3)`，三因子为原始值
  2. 对比三因子返回量级：`_slope_factor`（`ann_ret * r_sq`，O(0.1~1)）、`_bias_factor`（`slope * 100`，O(0.01~0.1)）、`_efficiency_factor`（`momentum * efficiency_ratio * 100`，O(0.5~5)）
  3. 运行 `python -c` 计算任一交易日三因子原始值与加权贡献，观察 F3 占比（实测 84.3%）
- **受影响文件**:
  - `etf-strategies/backtest/strategies/three_factor_momentum.py:121-127`（未归一化加权）
  - `etf-strategies/backtest/strategies/three_factor_momentum.py:67-85`（`_efficiency_factor` ×100 放大）
- **潜在影响**: S8 实际运行效果近似「效率动量单因子策略」，而非 KB 所述三因子互补加权。因子权重 0.4/0.3/0.3 形同虚设（F3 决定性），「三因子互补降低单因子失效风险」的 KB 卖点不成立。S8 回测年化 34.29% 与 S4 的 34.46% 接近，可能部分源于两者实际均为「趋势质量类」单一信号驱动，而非三因子叠加增益。另附发现：阈值保护条件 `holder_score > 0`（L137）使当前持仓得分为负时（全部负动量场景）跳过 1.5 倍阈值保护直接切换，KB 未说明该边界行为（属合理设计，仅需 KB 补充）
- **建议修复**: 人工裁决二选一：(A) 若设计意图是三因子互补 → 对三因子做滚动 z-score/Min-Max 归一化后再按 0.4/0.3/0.3 加权（避免使用全样本统计量造成前视），并修正代码注释；(B) 若接受现状（效率因子主导）→ 更新 KB S8 因子描述与权重含义，并修正代码注释的「标准化」表述。修改后重跑 `run_backtest.py` 对比 S8 指标

## 巡检证据
```
$ python -c "三因子量级实测（标准测试夹具 _prices_multi, 513100 最后25日窗口）"
F1 斜率动量 = 0.647898
F2 乖离动量 = 0.032384
F3 效率动量 = 4.795206
加权后 = 0.4*f1 + 0.3*f2 + 0.3*f3 = 1.707436
F3占加权分数比重 = 84.3%

# 源码关键段 (three_factor_momentum.py L121-126)
# 注释: "实际上三个因子原始量级不同，这里用简单的标准化"  ← 声称标准化但未执行
score = (self.w_slope * f1 +
         self.w_bias * f2 +
         self.w_efficiency * f3)

# KB S8 factors: "三因子归一化后加权" "① 斜率动量(40%) ② 乖离动量(30%) ③ 效率动量(30%)"
```

## 自动修复判定
- **判定时间**: 2026-08-31T14:15+08:00
- **判定结果**: 不自动修复 — 涉及计算逻辑/算法/金融公式（S8 因子归一化属策略打分公式，修改将改变信号行为）
- **建议**: 人工审阅后执行 bug_fix_template.md 流程
