# BUG-009: conftest.py 测试环境隔离缺陷 — setdefault 无法覆盖继承的 DB_MODE=memory，导致全量测试大面积失败+挂起+TOS 污染风险

- **组件**: tests
- **严重程度**: HIGH
- **违反的正确性定义**: 无直接对应 DEF（测试基础设施）；间接影响 DASH-002（SQLite 表存在性校验）、全部 dashboard/agent DB 相关定义的可回归性
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：测试环境配置问题（`conftest.py` 隔离防护不完整），修复为机械性确定性改动（setdefault → 强制赋值），无算法/口径判断
- **发现时间**: 2026-08-31T12:25 CST
- **发现方式**: 每日代码巡检
- **预期行为**: pytest 测试进程必须以 `DB_MODE=file` 运行（conftest.py 注释声明的契约 + `tests/test_test_isolation.py::test_conftest_sets_safe_env`/`test_db_not_in_memory_mode_under_pytest` 断言），隔离真实 cache.db 与 TOS
- **实际行为**: `tests/conftest.py` L21-23（2026-08-31 事故修复，未提交）使用 `os.environ.setdefault("DB_MODE", "file")`。当宿主环境已导出 `DB_MODE=memory`（本项目 `.env` 即 `DB_MODE=memory`，harness/Claude Code 会话常将其载入进程环境）时，`setdefault` 不生效，测试进程进入 memory 模式：
  - `dashboard/db.py`/`agent/db.py` 的 `USE_MEMORY=True` → `init_db()` 走 memory 分支，在模块内部 `_memory_conn()` 建表；而 `test_dashboard_db.py` fixture 打补丁的是另一个 `:memory:` 连接 → 查询空表 → **test_dashboard_db.py 全部 52 个测试失败**、agent_db/agent_knowledge/agent_lifecycle/agent_behavior/daily_review_sync 等 100+ 测试失败
  - memory 模式每次 `init_db()` 触发 `cloud_restore()`（真实 TOS 网络调用，cloud_store 可导入时）→ **测试套件挂起 >5 分钟**（实测卡在 60%）
  - 若 `cloud_backup()` 被触发，测试数据可能经 memory 快照回传扩散到 TOS —— 正是 2026-08-31 事故要防的场景，防护未闭合
- **复现步骤**:
  1. `cd etf-strategies && $env:DB_MODE='memory'; $env:CLOUD_RESTORE_ON_START='1'; python -m pytest tests/test_dashboard_db.py -q --tb=short` → 全部失败（`test_init_creates_all_tables`：`names == set()`）
  2. 同一命令在 `$env:DB_MODE='file'` 下 → 52 个测试全部通过（0.36s）→ 证明失败根因是 memory 模式泄漏
  3. `python -c "import dashboard.db as m; print(m.USE_MEMORY)"`（宿主环境）→ `True`
- **受影响文件**:
  - `etf-strategies/tests/conftest.py:21`（`os.environ.setdefault("DB_MODE", "file")` 不生效）
- **潜在影响**: 从任何已加载 `.env`（DB_MODE=memory）的父进程运行 pytest 时：全量测试 100+ 失败（误报大量 BUG）、套件挂起超过 5 分钟超时阈值、存在测试数据经 memory 快照回传污染 TOS 的风险；同时 `test_test_isolation.py` 的防护断言在真实环境不成立，防护形同虚设
- **建议修复**: 将 `conftest.py` L21 改为强制赋值 `os.environ["DB_MODE"] = "file"`（pytest 进程内强制文件模式，与 `test_db_memory.py` 通过 monkeypatch 设置 `USE_MEMORY=True` 的模式不冲突）；`CLOUD_RESTORE_ON_START` 同理评估（见 BUG-010）

## 巡检证据
```
$ $env:DB_MODE='memory'; python -m pytest tests/test_dashboard_db.py -q --tb=short
tests\test_dashboard_db.py F ...  →  52 collected, 大量 FAILED
E   AssertionError: assert False
E    +  where False = <built-in method issubset ...>(set())   # 表名为空集

$ $env:DB_MODE='file'; python -m pytest tests/test_dashboard_db.py -q
52 passed in 0.36s

$ python -c "import dashboard.db as m; print(m.USE_MEMORY)"   # 宿主环境
True

$ python -m pytest tests/ -v --tb=short   # 宿主环境(DB_MODE=memory)全量
... 卡在 tests/test_dashboard_db.py::TestNavCrud 60% 处，>5 分钟无进展（挂起），
    已按 bug_inspect_code 异常处理条款记录超时并终止
$ $env:DB_MODE='file' 全量重跑 → 400 passed, 1 failed (BUG-010), 2 skipped (网络) in 61.49s
```

## 自动修复判定
- **判定时间**: 2026-08-31T12:35+08:00
- **判定结果**: 自动修复 — 测试环境配置问题（conftest 隔离防护不闭合），机械性确定性修复
- **建议**: 修复后重跑 `python -m pytest tests/ -q` 确认 0 环境性失败、无挂起

## 自动修复记录
- **修复时间**: 2026-08-31T14:12+08:00
- **修复方式**: `tests/conftest.py` L21 `os.environ.setdefault("DB_MODE", "file")` → 强制赋值 `os.environ["DB_MODE"] = "file"`（pytest 进程内强制文件模式，覆盖宿主进程继承的 DB_MODE=memory；`test_db_memory.py` 经 monkeypatch 设置 USE_MEMORY 的模式不受影响）；CLOUD_RESTORE_ON_START 同步改为强制赋值（见 BUG-010）
- **修复结果**: ✅ 修复成功 — 宿主 DB_MODE=memory / CLOUD_RESTORE_ON_START=1 环境下，`test_conftest_sets_safe_env`/`test_db_not_in_memory_mode_under_pytest` 全部通过，无 memory 快照回传 / TOS 污染风险，无挂起
- **验证**: tests/test_load_env.py + tests/test_test_isolation.py → 8 passed；全量回归见下方
