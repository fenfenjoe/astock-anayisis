# BUG-017: bug_inspect_data 第四步巡检脚本缺陷 — 对策略类 hasattr('assets') 恒为 False，活跃ETF集合恒为空，数据完整性门永久失效

- **组件**: data
- **严重程度**: MEDIUM
- **违反的正确性定义**: DAT-001（数据完整性检查必须真实覆盖全部活跃策略 ETF — 巡检门不得静默假通过）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：机械性确定性修复 — 将脚本中对策略**类**的 `hasattr(s, 'assets')` 改为实例化后取 `assets`（复用 `list_strategies._default_assets(cls)` 或 `cls().assets`），属于巡检脚本本身缺陷，不涉及金融计算
- **发现时间**: 2026-09-01T12:10+08:00（2026-09-01 12:17 代码巡检确认复现路径更新：hasattr 因 base.py 类属性 `assets: list = []` 恒 True，但取到空列表，all_etfs 仍恒空）
- **发现方式**: 每日数据巡检 (bug_inspect_data) 第四步执行时发现
- **预期行为**: `bug_inspect_data.md` 第四步脚本收集 `daily_signal.py STRAT_MAP` 注册的活跃策略全部 ETF（应为 13 只：159915/510180/510300/510500/511260/511880/512010/512480/512660/512800/512880/513100/518880），逐一检查缓存存在性与最后数据日期新鲜度
- **实际行为**: `from list_strategies import STRATEGIES` 返回的是**策略类**列表（如 `BuyHold`），`hasattr(s, 'assets')` 对类恒为 False（assets 是实例属性，非类属性）→ `all_etfs` 恒为空集 → 脚本恒输出 `OK: 所有 0 个活跃 ETF 数据完整` 并退出码 0。**数据完整性门从未真实检查过任何 ETF**，活跃 ETF 缓存缺失/过期时会静默通过（假阴性）
- **复现步骤**:
  1. `cd etf-strategies && python -c "from list_strategies import STRATEGIES; print([(s.__name__, hasattr(s,'assets'), s.assets) for s in STRATEGIES[:5]])"` → `[('BuyHold', True, []), ('DualMomentum', True, []), ...]`
     - **2026-09-01 复现路径更新**：base.py 已定义类属性 `assets: list = []`，故 `hasattr(s,'assets')` 现恒为 True，但类属性的值是空列表 `[]`（assets 实例值需实例化后才有）→ `all_etfs` 仍恒为空集，缺陷实质不变
  2. 执行 bug_inspect_data.md 第四步原样脚本 → 输出 `OK: 所有 0 个活跃 ETF 数据完整`
  3. 修正为实例化后取 assets（`cls().assets` 或 `list_strategies._default_assets(cls)`） → 正确输出 `活跃策略 ETF 总数: 13`
- **受影响文件**:
  - `etf-strategies/automation/prompts/bug_inspect_data.md`（第四步内嵌检查脚本）
- **潜在影响**: 数据完整性巡检门失效 — 若某活跃策略 ETF 缓存文件被删除或数据滞后 >3 天，巡检不会告警，信号生成可能在数据缺失状态下继续运行。本次巡检即因该缺陷首次未发现任何问题，修正后才暴露真实的完整性状态（本次 13 只均完整，但门本身不可信）
- **建议修复**: 修改 `bug_inspect_data.md` 第四步脚本，改用 `list_strategies._default_assets(cls)` 或 `cls().assets`（try/except 兜底）收集活跃 ETF；修复后重跑确认输出 `OK: 所有 13 个活跃 ETF 数据完整`

## 巡检证据
```
$ python -c "..." (原样脚本)
OK: 所有 0 个活跃 ETF 数据完整

$ python -c "..." (2026-09-01 复现: hasattr 现为 True 但取到类属性空列表)
[('BuyHold', True, []), ('DualMomentum', True, []), ('MATrend', True, []), ...]
all_etfs = set() (len=0)

$ python -c "..." (修正后: from list_strategies import STRATEGIES, _default_assets; for cls in STRATEGIES: assets,_=_default_assets(cls) ...)
活跃策略 ETF 总数: 13
活跃 ETF 列表: ['159915', '510180', '510300', '510500', '511260', '511880', '512010', '512480', '512660', '512800', '512880', '513100', '518880']
OK: 所有 13 个活跃 ETF 数据完整
```

## 自动修复记录
- **修复时间**: 2026-09-01T13:15+08:00
- **修复方式**: 修改 `etf-strategies/automation/prompts/bug_inspect_data.md` 第四步内嵌检查脚本 — 弃用对策略**类**的 `hasattr(s, 'assets')`（base.py 类属性 `assets: list = []` 使 hasattr 恒 True 但取到空列表），改为 `from list_strategies import STRATEGIES, _default_assets` + `_default_assets(cls)` 实例化后取实例 assets（try/except 兜底）
- **修复结果**: ✅ 修复成功 — 修复后脚本实测输出 `活跃策略 ETF 总数: 13` + `OK: 所有 13 个活跃 ETF 数据完整`（与 BUG 报告预期完全一致），完整性门恢复真实检查能力
- **验证**: 修复后脚本逻辑实测通过；`pytest tests/` 全量回归通过（与本轮 BUG-016 修复一并验证）
