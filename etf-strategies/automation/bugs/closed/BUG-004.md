# BUG-004: cache/512100.parquet 缓存过期（最后修改 2026-07-22，>24h）

- **组件**: data
- **严重程度**: MEDIUM
- **违反的正确性定义**: DAT-001（缓存新鲜度 — 缓存文件最后修改时间应 ≤24h）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：缓存过期属机械性确定性修复范畴（自动执行 `a-stock-data` 重拉即可恢复新鲜度）
- **发现时间**: 2026-08-27T12:08 CST
- **发现方式**: 每日数据巡检 (bug_inspect_data)
- **预期行为**: `etf-strategies/cache/` 下所有 parquet 缓存文件最后修改时间不超过 24h（昨日前），保证信号生成与早盘分析基于最新数据
- **实际行为**: `etf-strategies/cache/512100.parquet` 最后修改时间为 **2026-07-22**（约 36 天前，远超 24h 阈值），为唯一过期缓存文件。512100（中证1000ETF）当前不在 `daily_signal.py STRAT_MAP` 注册的 13 个活跃策略资产池中，属非活跃 ETF
- **复现步骤**:
  1. `cd etf-strategies && python -c "import os,glob; from datetime import datetime,timedelta; yesterday=datetime.now().date()-timedelta(days=1); [print(f, datetime.fromtimestamp(os.path.getmtime(f)).date()) for f in glob.glob('cache/*.parquet')]"` → 仅 512100.parquet 的 mtime=2026-07-22 < yesterday(2026-08-26)
- **受影响文件**:
  - `etf-strategies/cache/512100.parquet`（最后修改 2026-07-22）
- **潜在影响**: 512100 当前不在活跃策略资产池（STRAT_MAP 13 个 ETF 均数据完整、最后日期 ≤1 天），不影响本日信号生成与早盘分析；但该 ETF 已注册在 `daily_signal.py ETF_NAMES` 且回测基准覆盖 S1~S19 均可能引用中证1000 资产，若未来策略启用将直接使用过期数据。建议尽快重拉恢复
- **建议修复**: 调用 `a-stock-data`（东财 kline / 腾讯财经）对 512100 重新拉取全量日K并写回 `cache/512100.parquet`（等价 `get_kline("512100", refresh=True)`），随后重跑本巡检确认 mtime 恢复

## 巡检证据
```
$ python -c "..." (缓存新鲜度检查)
STALE CACHE FILES:
  cache\512100.parquet (last: 2026-07-22)
Total: 1 stale

其余 13 个缓存文件 mtime ≥ 2026-08-26，全部新鲜
```

## 自动修复记录
- **修复时间**: 2026-08-27T14:08+08:00
- **修复方式**: `get_kline("512100", refresh=True)` 走东财 push2his（em_get 限流）重拉全量日K并写回 `cache/512100.parquet`
- **修复结果**: ✅ 修复成功 — 缓存 mtime 更新至 2026-08-27 14:07:59，数据覆盖 2016-11-04 ~ 2026-08-27（2384 行，最新收盘 3.1180 @ 2026-08-27），满足 DAT-001 ≤24h 新鲜度阈值
- **验证**: 重跑缓存新鲜度检查 → 无过期文件；全量回归 286 passed, 2 skipped
