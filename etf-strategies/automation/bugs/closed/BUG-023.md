# BUG-023: test_agent_behavior 3 个 run_post_pipeline 测试引用已删除函数 — 5c6fc61 重构遗留，AttributeError

- **组件**: tests
- **严重程度**: HIGH
- **违反的正确性定义**: 不适用（测试套件完整性 — 全量测试失败会阻断自动化闭环）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：5c6fc61（2026-09-04）重构将「发动态」功能并入 `execute_reading()`（`behavior.py:515-521` 以 `kind="post"` 写动态），`run_post_pipeline()` 被**有意移除**；3 个测试仍引用已删除函数 → 属「测试过时」机械性修复（删除/改写过期测试即可，不涉及金融计算/策略逻辑），同 BUG-018 修复模式（测试与实现同步）
- **发现时间**: 2026-09-07T12:25+08:00
- **发现方式**: 每日代码巡检 (bug_inspect_code) 第四步全量测试
- **预期行为**: 全量测试 0 FAILED；测试引用的 API 应存在于被测模块
- **实际行为**: 3 个测试因 `AttributeError: module 'agent.core.behavior' has no attribute 'run_post_pipeline'` 失败：

  | 测试 | 错误 |
  |------|------|
  | `tests/test_agent_behavior.py::test_run_post_pipeline_outside_active_hours` | AttributeError: no attribute 'run_post_pipeline' |
  | `tests/test_agent_behavior.py::test_run_post_pipeline_first_post` | AttributeError: no attribute 'run_post_pipeline' |
  | `tests/test_agent_behavior.py::test_run_post_pipeline_throttled` | AttributeError: no attribute 'run_post_pipeline' |

  **根因**：提交 `5c6fc61`（2026-09-04 13:24, dongyz8）将 `agent/core/behavior.py` 大规模重构（+247/-59），模块编排入口从 `run_publish_pipeline()/run_post_pipeline()` 改为 `run_publish_pipeline()/pick_random_state()/execute_reading()`，`run_post_pipeline()`（原「发动态：节流+活跃时段+dsh directive」）被删除，发动态能力由 `execute_reading()` 的 `kind="post"` 路径承接（behavior.py:515-521）。**但 test_agent_behavior.py（自 41c96b8 08-28 交付后未改动）中 3 个 `test_run_post_pipeline_*` 测试未被同步删除/改写** → 引用已删除函数。
  - 09-03 巡检（HEAD 在 5c6fc61 之前）测试全绿（435 passed）；09-04 重构后无代码巡检覆盖（09-04~09-06 无 code_inspect log），缺陷首次在本日（09-07）巡检暴露。
- **复现步骤**:
  1. `cd etf-strategies && python -m pytest tests/test_agent_behavior.py::test_run_post_pipeline_first_post -q --tb=short`
  2. 观察 `AttributeError: module 'agent.core.behavior' has no attribute 'run_post_pipeline'`
  3. `git log -S "def run_post_pipeline" -- etf-strategies/agent/core/behavior.py` → 确认 5c6fc61 删除
- **受影响文件**:
  - `etf-strategies/tests/test_agent_behavior.py:120-164`（3 个 test_run_post_pipeline_* 测试）
  - `etf-strategies/agent/core/behavior.py`（5c6fc61 已删 `run_post_pipeline`，现行为正确，无需改）
- **潜在影响**: 全量测试 10 failed + 5 error（本 BUG 占 3 failed），使自动化测试门禁持续红色，遮蔽后续真实回归；若自动化按「0 failed 才算通过」判定，会误判项目质量。生产代码本身不受影响（发动态能力已由 execute_reading 承接）。
- **建议修复**: 二选一：(a) 删除 3 个过时的 `test_run_post_pipeline_*` 测试（推荐，功能已并入 execute_reading，`test_execute_reading`/`test_agent_behavior.py:84-114 run_publish_pipeline` 已覆盖新路径）；(b) 若需保留独立发动态管线，从 5c6fc61 前一版本（41c96b8）恢复 `run_post_pipeline` 实现并重新接线。倾向 (a) — 删除过期测试。

## 巡检证据
```
$ python -m pytest tests/ -v --tb=short
> tests/test_agent_behavior.py::test_run_post_pipeline_outside_active_hours FAILED [  7%]
> tests/test_agent_behavior.py::test_run_post_pipeline_first_post FAILED   [  8%]
> tests/test_agent_behavior.py::test_run_post_pipeline_throttled FAILED    [  8%]
E   AttributeError: module 'agent.core.behavior' has no attribute 'run_post_pipeline'
tests\test_agent_behavior.py:125: in test_run_post_pipeline_outside_active_hours
      res = behavior.run_post_pipeline(now=now, llm_fn=lambda t: POST_TEXT)

$ git show 5c6fc61 -- etf-strategies/agent/core/behavior.py
  编排入口：run_publish_pipeline()（每日发文）、run_post_pipeline()（动态）。
  → 编排入口：run_publish_pipeline()（每日发文）、pick_random_state()（状态决策）、execute_reading()（阅读 Prompt 执行）。
  -def run_post_pipeline(now=None, llm_fn=None, db=None, ...
  （run_post_pipeline 整体删除，发动态并入 execute_reading 的 kind="post" 路径）

$ git log -S "def run_post_pipeline" -- etf-strategies/agent/core/behavior.py
5c6fc61 2026-09-04 13:24:35 +0800 commit   ← 删除
41c96b8 2026-08-28 14:25:25 +0800 feat(agent) ← 引入
```

## 关联
- 与 BUG-024 同源：09-04 两笔重构（5c6fc61 / 3bcc9d0）改动实现未同步测试，09-04~09-06 无代码巡检，首次在本日暴露。

## 自动修复记录（bug_auto_fix 2026-09-07 13:22 轮）
- **修复时间**: 2026-09-07T13:22+08:00
- **修复方式**: 按建议方案 (a) 删除 3 个过时的 `test_run_post_pipeline_*` 测试（`tests/test_agent_behavior.py`）。功能已并入 `execute_reading()` 的 `kind="post"` 路径（behavior.py:515-521），新路径由 `tests/test_agent_activity.py` 的 execute_reading 测试覆盖。
- **修复结果**: ✅ 修复成功 — `python -m pytest tests/test_agent_behavior.py tests/test_agent_lifecycle.py` **15 passed**；全量回归 460 passed（较 BUG-025 基线 458 passed +2：本 BUG -3 failed + BUG-024 -1 failed +1 new passed），3 个 AttributeError 全部消除
- **回滚**: 改动仅删除过期测试 + 注释说明，可经 git revert 恢复
