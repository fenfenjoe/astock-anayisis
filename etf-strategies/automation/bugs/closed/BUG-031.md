# BUG-031: test_scheduler_status `_clean_running` fixture 未重置 `agent_online()` 的 `_online_cache` — 测试跨用例缓存污染（顺序相关失败）

- **组件**: tests
- **严重程度**: MEDIUM
- **违反的正确性定义**: 不适用（测试隔离缺陷 — 非 backtest 组件；`agent_online()` 生产代码的 5s 短缓存为有意设计）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：确定性机械修复 — 在 `_clean_running` fixture 中同时重置 `sched._online_cache.ts = 0.0`（清空 5s 短缓存），使 3 个 `agent_online` 用例互不污染。属测试隔离修正，不涉及公式/算法/策略行为
- **发现时间**: 2026-09-08T12:25+08:00
- **发现方式**: 每日代码巡检 (bug_inspect_code) 第四步全量测试
- **预期行为**: 3 个 `agent_online` 用例各自独立、与执行顺序无关：
  - agent db 返回 `"1"` → `agent_online()` 返回 True
  - agent db 返回 `"0"` → 返回 False
  - agent db 抛异常 → fail-safe 返回 False
- **实际行为**: 全量运行中 `test_agent_online_false_when_agent_db_says_0` 与 `test_agent_online_false_when_agent_db_unavailable` FAILED（`assert True is False`）；单跑 3 个用例（调整顺序）时变为 `test_agent_online_true_when_agent_db_says_1` FAILED（`assert False is True`）— **失败用例随执行顺序漂移**。
- **根因**: `dashboard/scheduler.py:102,164-177` 的 `agent_online()` 带 `_online_cache`（`_ONLINE_CACHE_SECONDS = 5`），首个触发用例把结果缓存进模块级 `_online_cache`（`_SimpleCache`，`ts`/`val`）；`tests/test_scheduler_status.py:12-16` 的 autouse fixture `_clean_running` 只重置 `sched._running_tasks = {}`，**未重置 `_online_cache`** → 后续用例在 5s 缓存窗口内命中上一次用例的旧值（True 或 False），monkeypatch 的 `agent.db.meta_get` 根本未被调用 → 断言与缓存值不符而失败。生产代码行为符合设计（5s 短缓存防 `_tick` 高频轮询反复连库），问题在测试隔离。
- **复现步骤**:
  1. 全量：`cd etf-strategies && python -m pytest tests/test_scheduler_status.py -v --tb=short` → `test_agent_online_true_when_agent_db_says_1` 先跑写入 True 缓存，后 2 个 false 用例命中缓存 FAILED（`assert True is False`）
  2. 改变顺序：`python -m pytest tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_unavailable tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_says_0 tests/test_scheduler_status.py::test_agent_online_true_when_agent_db_says_1 -v` → false 用例先写 False 缓存，`true_says_1` 命中缓存 FAILED（`assert False is True`）
  3. 对照（fixture 重置缓存后）：3 用例应全部 PASS
- **受影响文件**:
  - `etf-strategies/tests/test_scheduler_status.py:12-16`（`_clean_running` fixture 缺 `_online_cache` 重置）
  - `etf-strategies/dashboard/scheduler.py:91,94-102,164-177`（`_online_cache` / `_SimpleCache` / `agent_online()` 5s 缓存 — 生产代码无需改动）
- **潜在影响**: 测试结果不可靠（同一文件内用例相互依赖执行顺序），遮蔽 `agent_online()` 回归（如后续改 fail-safe 逻辑）；全量门禁红色。对生产调度无影响（`_tick` 每 20s 调一次、5s 缓存窗口内至多一次陈旧读，为有意设计）。
- **建议修复**: `_clean_running` fixture 中追加 `sched._online_cache.ts = 0.0`（或 `sched._online_cache = _SimpleCache()` 重置），置于 monkeypatch/用例前后各一次，消除跨用例缓存污染。无需改动 `agent_online()` 生产代码。

## 巡检证据
```
$ python -m pytest tests/ --cov=backtest --cov-report=term -q | tail   （全量，test_scheduler_status 内默认顺序）
FAILED tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_says_0 - assert True is False
FAILED tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_unavailable - assert True is False
   +  where True = <function agent_online at 0x...>()
   +    where <function agent_online at 0x...> = sched.agent_online

$ python -m pytest tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_unavailable tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_says_0 tests/test_scheduler_status.py::test_agent_online_true_when_agent_db_says_1 -v
tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_unavailable PASSED
tests/test_scheduler_status.py::test_agent_online_false_when_agent_db_says_0 PASSED
tests/test_scheduler_status.py::test_agent_online_true_when_agent_db_says_1 FAILED - assert False is True
   +  where False = <function agent_online at 0x...>()
（失败用例随顺序漂移 → 确认 _online_cache 跨用例污染）
```

## 自动修复记录（bug_auto_fix 2026-09-08 13:09 轮）
- **修复时间**: 2026-09-08T13:09+08:00
- **修复方式**: 按建议修复方案，在 `tests/test_scheduler_status.py:12-20` 的 autouse fixture `_clean_running` 中追加 `sched._online_cache.ts = 0.0; sched._online_cache.val = False`（用例前/后各一次），消除 `agent_online()` 5s 短缓存跨用例污染。**未改动 `agent_online()` 生产代码**（5s 短缓存为有意设计，`_tick` 每 20s 一次，符合预期）。
- **修复结果**: ✅ 修复成功 — 修复前全量 `test_scheduler_status.py` 2 failed（`assert True is False`，顺序相关）；修复后 9/9 全绿，且 3 个 `agent_online` 用例以相反顺序单跑仍全部 PASSED（顺序无关）。
- **回滚**: 改动仅涉及测试 fixture（新增 2 行缓存重置），可经 git revert 恢复；生产代码零改动。
