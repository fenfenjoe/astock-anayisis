# BUG-018: CLI 帮助文本/文档字符串仍写「S1-S11」，实际已支持 S1-S19（S12-S19 遗漏提示）

- **组件**: registration
- **严重程度**: LOW
- **违反的正确性定义**: REG-002 相关（STRAT_MAP 已注册 S1-S19，但 CLI 帮助与文档字符串仍声称仅 S1-S11）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：机械性确定性修复 — 仅需将 4 处帮助字符串/文档字符串中的「S1-S11/S1~S11」改为「S1-S19」，不涉及任何金融计算/策略行为，属文档一致性修正
- **发现时间**: 2026-09-02T12:26+08:00
- **发现方式**: 每日代码巡检（第二步静态分析 — 注册一致性检查时抽查 daily_signal.py / cli.py 帮助文本）
- **预期行为**: CLI 帮助文本与 docstring 反映实际支持的全部策略编号 S1-S19
- **实际行为**: 4 处用户可见的 CLI help 与 docstring 仍写「S1-S11」/「S1~S11」，与实际注册的 S1-S19 不一致：
  - `daily_signal.py:248` — argparse help `"策略编号 (S1-S11) 或 '--all'"`
  - `cli.py:282` — argparse help `"策略编号 (S1~S11)，不填则列出可用策略"`（learn 子命令）
  - `cli.py:286` — argparse help `"策略编号 (S1~S11)，不填则列出可用策略"`（report 子命令）
  - `strategy_kb.py:435` — `get_kb()` docstring `"strategy_id: S1~S11。"`
  而 `daily_signal.py:40-60` STRAT_MAP 已注册 S1-S19（S12 量价情绪/S13 多因子/S14 动态波动率/S15 趋势过滤/S16 金丝雀/S17 金丝雀日频/S18 RSRS反转/S19 低相关），`cli.py` 与 `strategy_kb.py` 也均支持这些编号。
- **复现步骤**:
  1. `cd etf-strategies && python daily_signal.py --help` → 输出 `usage: ... strategy [策略编号 (S1-S11) 或 '--all']`
  2. `cd etf-strategies && python cli.py learn --help` / `python cli.py report --help` → 输出 `策略编号 (S1~S11)，不填则列出可用策略`
  3. `grep -n "S1-S11\|S1~S11" daily_signal.py cli.py strategy_kb.py` → 4 处命中，而实际 STRAT_MAP 支持 S1-S19
- **受影响文件**:
  - `etf-strategies/daily_signal.py:248`
  - `etf-strategies/cli.py:282,286`
  - `etf-strategies/strategy_kb.py:435`
- **潜在影响**: 极低。不影响任何代码逻辑/回测/信号（STRAT_MAP 本身正确），仅 CLI 帮助文本与文档字符串误导用户以为只支持 S1-S11；用户可能因帮助文本提示而不敢输入 S12-S19（实际可用）。属用户体验/文档一致性问题
- **建议修复**: 将 4 处「S1-S11/S1~S11」统一改为「S1-S19」，保持与 STRAT_MAP / KB / STRATEGIES 实际支持范围一致

## 巡检证据
```
$ grep -n "S1-S11\|S1~S11" daily_signal.py cli.py strategy_kb.py
etf-strategies/daily_signal.py:248:  help="策略编号 (S1-S11) 或 '--all'")
etf-strategies/cli.py:282:       help="策略编号 (S1~S11)，不填则列出可用策略")
etf-strategies/cli.py:286:       help="策略编号 (S1~S11)，不填则列出可用策略")
etf-strategies/strategy_kb.py:435: """获取策略知识。strategy_id: S1~S11。"""

# 实际支持范围 (daily_signal.py STRAT_MAP)
S1~S19 全部注册（S12 量价情绪多因子 ... S19 低相关ETF轮动）

# cli.py signal 子命令遍历 STRAT_MAP.items()，S12-S19 均可用
# strategy_kb.KB 含 S1-S19 全部 19 条（grep ^\"S[0-9]*\": 19 命中）
```

## 自动修复判定
- **判定时间**: 2026-09-02T12:26+08:00
- **判定结果**: 可自动修复 — 机械性确定性文档修正（4 处字符串 S1-S11→S1-S19），不涉及金融计算
- **建议**: 由 bug_auto_fix 执行字符串替换后重跑 grep 确认 0 命中

## 自动修复记录
- **修复时间**: 2026-09-02T13:07+08:00
- **修复方式**: 将 4 处「S1-S11/S1~S11」统一改为「S1-S19」（与 STRAT_MAP 实际注册 S1-S19 一致）：
  - `daily_signal.py:248` argparse help `"策略编号 (S1-S11) 或 '--all'"` → `"策略编号 (S1-S19) 或 '--all'"`
  - `cli.py:282` learn 子命令 help `"策略编号 (S1~S11)，不填则列出可用策略"` → `"策略编号 (S1~S19)，不填则列出可用策略"`
  - `cli.py:286` report 子命令 help `"策略编号 (S1~S11)，不填则列出可用策略"` → `"策略编号 (S1~S19)，不填则列出可用策略"`
  - `strategy_kb.py:435` `get_kb()` docstring `"strategy_id: S1~S11。"` → `"strategy_id: S1~S19。"`
- **修复结果**: ✅ 修复成功 — 修改后 `grep -n "S1-S11\|S1~S11" daily_signal.py cli.py strategy_kb.py` 返回 0 命中；3 个文件语法检查通过；STRAT_MAP 确认注册 S1-S19（S12 量价情绪/S13 多因子/S14 动态波动率/S15 趋势过滤/S16 金丝雀/S17 金丝雀日频/S18 RSRS反转/S19 低相关）
- **验证**: `pytest tests/` 全量回归通过 — **415 passed, 1 skipped**（61.19s），无失败无回归
