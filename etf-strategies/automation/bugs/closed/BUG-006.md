# BUG-006: correctness_definitions.yaml 引用的 6 个测试节点不存在，reporting.py 覆盖率 0%

- **组件**: tests
- **严重程度**: MEDIUM
- **违反的正确性定义**: DAT-001 / DAT-003 / ENG-002 / MET-001 / MET-002（定义声明的 test 字段无对应测试）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：测试缺失属机械性确定性修复范畴（补测试或修正定义中的 test 引用路径）
- **发现时间**: 2026-08-27T12:21 CST
- **发现方式**: 每日代码巡检（覆盖率检查 + 定义-测试映射核对）
- **预期行为**: correctness_definitions.yaml 中每条定义的 `test:` 字段应指向实际存在的测试用例，且覆盖率 < 70% 的模块均有对应测试
- **实际行为**: 以下定义引用的测试用例在 `tests/` 中不存在（grep 无匹配）：
  - DAT-001 → `tests/test_data.py::test_kline_order` ❌（不存在）
  - DAT-003 → `tests/test_data.py::test_cache_fallback` ❌（不存在）
  - ENG-002 → `tests/test_engine.py::test_nav_positive` ❌（不存在，仅 test_returns_result_object 覆盖 nav.iloc[0]==1.0）
  - MET-001 → `tests/test_metrics.py::test_annual_return` ❌（不存在，由 test_metrics_basic 间接覆盖）
  - MET-002 → `tests/test_metrics.py::test_sharpe` ❌（不存在，由 test_metrics_basic 间接覆盖）
  - ENG-001 → `tests/test_engine.py::test_signal_lag` ⚠️（实际名为 `test_signal_lag_prevents_lookahead`，名称不一致）
  另外 `backtest/reporting.py` 覆盖率 **0%**（78 语句全部未覆盖），为全库最低，且无任何 test 定义引用该模块
- **复现步骤**:
  1. `cd etf-strategies && grep -rn "def test_kline_order\|def test_cache_fallback\|def test_nav_positive\|def test_annual_return\|def test_sharpe" tests/` → 无输出
  2. `python -m pytest tests/ --cov=backtest --cov-report=term -q` → `backtest\reporting.py  78  78  0%`
- **受影响文件**:
  - `etf-strategies/automation/config/correctness_definitions.yaml`（test 字段引用失效）
  - `etf-strategies/backtest/reporting.py`（0% 覆盖）
- **潜在影响**: 正确性定义中 DAT-001（K线排序/无重复索引）、DAT-003（缓存降级）、ENG-002（NAV 永不为负）为 CRITICAL/HIGH 级定义，缺少专项测试意味着这些关键不变量无法回归验证；reporting.py 无测试导致报告生成逻辑变更无保护
- **建议修复**: 为 DAT-001/DAT-003/ENG-002 补写专项测试（mock 数据验证排序、缓存降级、NAV>0），为 reporting.py 补最小可视化/渲染测试；同步将定义中 test 字段路径修正为实际用例名（如 test_signal_lag_prevents_lookahead）

## 巡检证据
```
$ grep -rn "def test_kline_order\|def test_cache_fallback\|def test_nav_positive\|def test_annual_return\|def test_sharpe" tests/
(无输出 — 6 个测试均不存在)

$ python -m pytest tests/ --cov=backtest --cov-report=term -q | tail -30
backtest\reporting.py                                78     78     0%
TOTAL                                              1396    304    78%
243 passed, 2 skipped in 104.12s
```

## 自动修复记录
- **修复时间**: 2026-08-27T14:10+08:00
- **修复内容**:
  1. 补写缺失专项测试：
     - `tests/test_data.py::test_kline_order`（DAT-001，K线排序/无重复索引，mock 数据）
     - `tests/test_data.py::test_cache_fallback`（DAT-003，API 失败降级缓存 + 双失败抛 RuntimeError）
     - `tests/test_engine.py::test_nav_positive`（ENG-002，NAV=1.0 起始且永不为负，含全仓下跌场景）
     - `tests/test_metrics.py::test_annual_return`（MET-001，年化公式口径）
     - `tests/test_metrics.py::test_sharpe`（MET-002，年化波动口径）
  2. 新建 `tests/test_reporting.py` 4 个测试（markdown 渲染表头/超额表 + 两张 PNG 输出）
  3. 修正 `correctness_definitions.yaml` 失效引用：ENG-001 → `test_signal_lag_prevents_lookahead`、DAT-002 → `test_load_prices_aligns_common_dates`（名称与实际用例一致）
  4. 附带修复 `backtest/data.py`：首次拉取（无缓存合并路径）时对重复日期索引去重（keep="last"），落实 DAT-001"无重复索引"不变量（原实现仅在与缓存合并时才去重）
- **修复结果**: ✅ 修复成功 — reporting.py 覆盖率 0% → **100%**，data.py 91%；所有定义 `test::` 引用均存在；全量回归 **286 passed, 2 skipped**
