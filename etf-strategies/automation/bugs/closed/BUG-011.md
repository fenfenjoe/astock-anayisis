# BUG-011: 硬编码回测数据窗口 end=2026-07-01 过时 — 回测静默缺失 2026-07~08 数据，KB/sync 元数据同步过时

- **组件**: data
- **严重程度**: MEDIUM
- **违反的正确性定义**: DAT-001（get_kline 返回数据覆盖请求范围）/ DAT-005（数据路径与窗口统一管理）精神；与 BUG-008（缓存过期）同属数据新鲜度问题家族，但为代码层硬编码
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：硬编码日期常量（类 DAT-005 路径硬编码模式），修复为机械性确定性改动（滚动至当前日期或动态计算）
- **发现时间**: 2026-08-31T12:20 CST
- **发现方式**: 每日代码巡检（静态分析发现硬编码日期）
- **预期行为**: 回测入口与默认取数窗口应覆盖到当前交易日（2026-08-31），保证回测/报告/dashboard 指标基于最新数据
- **实际行为**: 以下位置硬编码结束日期 `2026-07-01`（距今已过 2 个月）：
  - `backtest/data.py:11` — `get_kline(code, start="2012-05-28", end="2026-07-01", ...)` 默认 end 过时（当前无调用者依赖默认值，属潜伏隐患）
  - `run_backtest.py:29` — `START, END = "2012-05-28", "2026-07-01"` 活跃使用：`python run_backtest.py` 回测窗口止于 07-01，**静默缺失 2026-07~08 两个月行情**
  - `run_backtest.py:110` — `render_markdown_report(..., data_end="2026-07-01")` 报告标注窗口过时
  - `strategy_kb.py:23~428` — 13 处 `"window": "... ~ 2026-07-01（约N年）"` KB 元数据过时
  - `dashboard/sync.py:40~132` — 13 处 `"window": "... ~ 2026-07-01"` 种子元数据过时
  - `dashboard/db.py:188` — 注释示例窗口过时
- **复现步骤**:
  1. `cd etf-strategies && grep -rn "2026-07-01" run_backtest.py backtest/data.py strategy_kb.py dashboard/sync.py` → 全部输出上述过时日期
  2. `python run_backtest.py`（联网时）→ 数据窗口 `data_end` 止于 2026-07-01，无 07-08 数据
  3. 对比 `daily_signal.py`（用 `date.today()` 动态窗口）→ 实盘信号窗口正确，与回测窗口脱节
- **受影响文件**:
  - `etf-strategies/run_backtest.py:29,110`
  - `etf-strategies/backtest/data.py:11`
  - `etf-strategies/strategy_kb.py:23,45,67,89,111,133,155,177,199,221,243,405,428`
  - `etf-strategies/dashboard/sync.py:40,45,50,55,60,64,68,73,78,83,88,124,132`
- **潜在影响**: 任何基于 run_backtest.py 的回测结果/报告/dashboard 策略指标静默缺失最近 2 个月行情，策略评估与新策略验证基于过期窗口；若未来有调用方使用 `get_kline(code)` 默认 end 将直接拿到截止 07-01 的数据（同 BUG-008 缓存过期影响面）
- **建议修复**: 将 `run_backtest.py` 的 `END` 与 `data.py` 的 `get_kline` 默认 end 改为动态（如 `date.today().strftime("%Y-%m-%d")`，同 `daily_signal.py` 模式），并同步刷新 `strategy_kb.py`/`dashboard/sync.py` 的 window 元数据描述（或改为占位符由运行时生成）

## 巡检证据
```
$ grep -rn "2026-07-01" run_backtest.py backtest/data.py strategy_kb.py dashboard/sync.py
run_backtest.py:29:  START, END = "2012-05-28", "2026-07-01"
run_backtest.py:110:  data_start="2012-05-28", data_end="2026-07-01"
backtest/data.py:11:  def get_kline(code, start="2012-05-28", end="2026-07-01", refresh=False):
strategy_kb.py:23:  "window": "2012-05-28 ~ 2026-07-01（约14年）",
...（共 13 处）
dashboard/sync.py:40:  "window": "2012-05-28~2026-07-01 (约14年)",
...（共 13 处）

$ Get-Date → 2026-08-31（当前日期，窗口已过期 2 个月）
```

## 自动修复判定
- **判定时间**: 2026-08-31T12:35+08:00
- **判定结果**: 自动修复 — 硬编码日期常量更新（机械性），同 DAT-005 路径硬编码修复模式
- **建议**: 修复后重跑 `python run_backtest.py --dry` 或核对 data_end 标注，确认窗口覆盖至当前交易日

## 自动修复记录
- **修复时间**: 2026-08-31T14:13+08:00
- **修复方式**:
  - `run_backtest.py`: `END` 改为 `date.today().strftime("%Y-%m-%d")`（当前 2026-08-31），`render_markdown_report` 改传 `data_start=START, data_end=END`
  - `backtest/data.py`: `get_kline` 默认 `end=None` → 运行时取当前日期（保持默认参数签名向后兼容，既有调用方显式传 end 不受影响）
  - `strategy_kb.py`（13 处）/ `dashboard/sync.py`（13 处）window 元数据、`dashboard/db.py:188` 注释示例同步刷新至 2026-08-31（"约N年"标注经核算不变）
- **修复结果**: ✅ 修复成功 — 回测窗口覆盖至当前日期，不再静默缺失 07~08 月行情；KB/sync 元数据与动态窗口一致
- **验证**: `grep -rn "2026-07-01" *.py` 仅剩修复说明注释；smoke 确认 `get_kline` 默认 end=None、run_backtest START/END = 2012-05-28/2026-08-31；策略/引擎/指标测试 114 passed
