# BUG-024: test_agent_lifecycle tick 测试未适配 3bcc9d0「上线/下线闸门」— 未设 xiaoman_online=1 致 RSS 断言失败

- **组件**: tests
- **严重程度**: HIGH
- **违反的正确性定义**: 不适用（测试套件完整性 — 实现变更未同步测试）
- **状态**: OPEN
- **auto_fix_eligible**: true
  - 判定理由：3bcc9d0（2026-09-04 17:49）在 `tick()` 新增「上线/下线闸门」（`xiaoman_online != "1"` → 只写 heartbeat 即返回），属**有意的行为契约变更**；测试未同步设置 `xiaoman_online=1` → 属「测试过时」机械性修复（在测试中按新契约初始化状态即可），不涉及金融计算/策略逻辑
- **发现时间**: 2026-09-07T12:25+08:00
- **发现方式**: 每日代码巡检 (bug_inspect_code) 第四步全量测试
- **预期行为**: `test_tick_writes_heartbeat_and_rss_throttle` 在 fresh tmp DB 上调用 `tick()` 应执行 RSS 拉取（`r1["rss"] is not None`），因为测试 monkeypatch 了 `fetch_and_store` 并期望它被调用一次
- **实际行为**: 测试失败 `assert r1["rss"] is not None` → `None`。根因：提交 `3bcc9d0`（2026-09-04 17:49, dongyz8）在 `agent/core/lifecycle.py tick()` 第 106-113 行新增上线/下线闸门：

  ```python
  online = db.meta_get("xiaoman_online")
  if online != "1":
      result["online"] = False
      return result
  ```

  fresh tmp DB 中 `xiaoman_online` 未设置（≠"1"）→ `tick()` 只写 heartbeat 后立即返回，`r1["rss"]` 保持 `None` → 断言失败。**测试文件 test_agent_lifecycle.py 自 41c96b8（08-28）后未改动**，未按新契约在 `tick()` 前设置 `xiaoman_online="1"`。
  - 09-03 巡检（HEAD 在 3bcc9d0 之前）测试全绿；09-04 加入闸门后无代码巡检覆盖（09-04~09-06 无 code_inspect log），首次在本日（09-07）暴露。
  - 对比：`test_scheduler_status.py:66-80`（09-07 同步修改）已按新契约 mock `xiaoman_online` 为 "1"/"0"，证明新契约是预期行为，只是 lifecycle 测试未同步。
- **复现步骤**:
  1. `cd etf-strategies && python -m pytest tests/test_agent_lifecycle.py::test_tick_writes_heartbeat_and_rss_throttle -q --tb=short`
  2. 观察 `assert r1["rss"] is not None` 失败（`r1["rss"]` 为 None）
  3. `git show 3bcc9d0 -- etf-strategies/agent/core/lifecycle.py` → 确认新增在线闸门
- **受影响文件**:
  - `etf-strategies/tests/test_agent_lifecycle.py:25-48`（test_tick_writes_heartbeat_and_rss_throttle）
  - `etf-strategies/agent/core/lifecycle.py:106-113`（3bcc9d0 新增闸门，现行为正确，无需改）
- **潜在影响**: 全量测试 1 个 FAILED，连同 BUG-023 使测试门禁红色；生产行为正确（上线闸门是预期功能，dashboard 通过 `api_agent.py /online`、`/offline` 写 `xiaoman_online`）。
- **建议修复**: 在测试 `tick()` 调用前设置 `agent_db.meta_set("xiaoman_online", "1")`（或 fixture 初始化），使测试符合新契约（参照 test_scheduler_status.py:66-80 的做法）；同时可补一条「下线时 tick 只写 heartbeat」的断言以覆盖新闸门。

## 巡检证据
```
$ python -m pytest tests/ -v --tb=short
> tests/test_agent_lifecycle.py::test_tick_writes_heartbeat_and_rss_throttle FAILED [ 16%]
> tests/test_agent_lifecycle.py:38: in test_tick_writes_heartbeat_and_rss_throttle
      assert r1["rss"] is not None
E   assert None is not None

$ git show 3bcc9d0 -- etf-strategies/agent/core/lifecycle.py
  +    # 0. 上线/下线闸门
  +    online = db.meta_get("xiaoman_online")
  +    if online != "1":
  +        result["online"] = False
  +        return result
  +    result["online"] = True

$ git log --format="%h %ci" --all -- etf-strategies/agent/core/lifecycle.py
93daee7 2026-09-06 21:50   ← HEAD
3bcc9d0 2026-09-04 17:49   ← 新增在线闸门
5c6fc61 2026-09-04 13:24
```

## 关联
- 与 BUG-023 同源：09-04 重构实现未同步测试；test_scheduler_status.py:66-80 已示范新契约适配方式。

## 自动修复记录（bug_auto_fix 2026-09-07 13:22 轮）
- **修复时间**: 2026-09-07T13:22+08:00
- **修复方式**: 在 `test_tick_writes_heartbeat_and_rss_throttle` 的 `tick()` 调用前设置 `agent_db.meta_set("xiaoman_online", "1")`（符合 3bcc9d0 新契约，参照 test_scheduler_status.py 做法）；并新增 `test_tick_offline_writes_only_heartbeat` 覆盖「下线时 tick 只写 heartbeat，RSS/发文全跳过」新闸门。
- **修复结果**: ✅ 修复成功 — `python -m pytest tests/test_agent_lifecycle.py` 2 passed（含新测试）；全量回归 460 passed（较 BUG-025 基线 458 passed +2），原 `assert r1["rss"] is not None` 失败已消除
- **回滚**: 改动仅测试文件，可经 git revert 恢复
