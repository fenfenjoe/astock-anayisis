# BUG-016: 缓存 2026-08-31 日K为盘中快照，11/18 缓存跨源价差 >0.5%（9/13 活跃ETF受影响）

- **组件**: data
- **严重程度**: HIGH
- **违反的正确性定义**: DAT-001（缓存数据应反映真实收盘价 — 信号生成与早盘分析基于最新、准确的数据）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：跨源价差根因已排查确认 = 缓存 08-31 bar 为**盘中快照**（08-31 交易时段 refresh 写入 partial bar，收盘后未重拉覆盖）。东财源本身正确（直接探针验证 4 只 08-31 收盘 510300=4.685 / 512480=1.058 / 513100=2.223 / 159915=3.456，与腾讯昨收、百度K线三源一致）。修复 = 对受影响缓存 `get_kline(code, refresh=True)` 确定性重拉（东财返回完整 08-31 bar 后按 data.py merge 逻辑 `keep="last"` 覆盖 partial bar），同 BUG-004/BUG-008 模式。
- **发现时间**: 2026-09-01T12:10+08:00
- **发现方式**: 每日数据巡检 (bug_inspect_data) 第三步跨源价格交叉验证
- **预期行为**: 缓存 parquet 最后一根日K（2026-08-31）应为当日真实收盘价，与腾讯财经/百度K线/东财API一致（差异 <0.1%）
- **实际行为**: 全部 18 个缓存文件的 2026-08-31 bar 为盘中快照（写入于 08-31 交易时段 09:37/10:53~10:57/14:10/16:27，均非收盘后完整日K），收盘价与真实值偏差 0.06%~3.59%：
  | 文件 | 缓存08-31 close | 真实08-31 close(东财/腾讯/百度) | 偏差 |
  |------|----------------|-------------------------------|------|
  | `cache/512480.parquet` | 1.020 | 1.058 | **-3.59%** |
  | `cache/510500.parquet` | 7.783 | 7.967 | **-2.31%** |
  | `cache/512660.parquet` | 1.118 | 1.142 | **-2.10%** |
  | `cache/510300.parquet` | 4.626 | 4.685 | **-1.26%** |
  | `cache/159915.parquet` | 3.413 | 3.456 | **-1.24%** |
  | `cache/512880.parquet` | 1.091 | 1.105 | **-1.27%** |
  | `cache/513100.parquet` | 2.204 | 2.223 | **-0.85%** |
  | `cache/510180.parquet` | 3.960 | 3.991 | **-0.78%** |
  | `cache/159326.parquet` | 1.661 | 1.670 | **-0.54%** |
  | `cache/512010.parquet` | 0.374 | 0.376 | **-0.53%** |
  | `cache/512100.parquet` | 3.133 | 3.149 | **-0.51%** |
  | `cache/159227.parquet` | 1.012 | 1.015 | -0.30%（WARN） |
  | `cache/601398.parquet` | 8.050 | 8.070 | -0.25%（WARN） |
  | `cache/518880.parquet` | 9.149 | 9.135 | +0.15%（WARN） |
  | `cache/512800.parquet` | 0.828 | 0.829 | -0.12%（WARN） |
  | `cache/002142.parquet` | 34.100 | 34.120 | -0.06%（WARN） |
  | `cache/511260.parquet` | 135.660 | 135.694 | -0.03%（OK） |
  | `cache/511880.parquet` | 100.752 | 100.751 | +0.001%（OK） |
- **复现步骤**:
  1. `cd etf-strategies && python -c "import pandas as pd; df=pd.read_parquet('cache/512480.parquet'); print(df.tail(1))"` → close=1.020, high=1.034（high < 真实收盘 1.058，结构性矛盾）
  2. 腾讯财经 `qt.gtimg.cn/q=sh512480` 昨收字段 = 1.058（三源一致）
  3. 东财 push2his `eastmoney_kline('512480', start='20260825', end='20260831')` → 08-31 close=1.058
- **受影响文件**:
  - `etf-strategies/cache/*.parquet`（全部 18 个，511260/511880 偏差可忽略仍建议重拉）
- **潜在影响**: `daily_signal.py load_prices()` 走 `get_kline(refresh=False)` 直接读缓存 → 今日信号基于错误的 08-31 收盘价。**9/13 活跃策略 ETF 最后收盘价偏差 0.5%~3.6%**，如 512480 真实日涨 +1.83% 被算成 -1.83%（方向反转），动量/趋势/轮动类因子（S2/S3/S4/S8/S9/S12/S13/S14~S19）的收益计算与信号判断实质失真；回测（run_backtest refresh=False）与 dashboard `refresh=False` 读取路径同样受影响
- **建议修复**: 调用 `a-stock-data`（东财 push2his kline）对全部受影响缓存 `get_kline(code, refresh=True)` 重拉并写回 parquet（同 BUG-008 修复模式），随后重跑本巡检第三步确认跨源价差 <0.1%
- **流程根因（建议后续 REQ 关注）**: 缓存 refresh 发生在交易时段（09:37/10:53/14:10），收盘后（15:00+）无刷新任务兜底 → 盘中快照 bar 成为"最后日K"。建议在数据刷新入口（data.py get_kline 或调度）增加"非交易时段才写当日 bar / 收盘后强制补全当日 bar"的防护

## 巡检证据
```
$ python -c "..." (跨源验证 抽样3只)
510300 沪深300ETF: 东财缓存收盘=4.6260(2026-08-31) vs 腾讯昨收=4.6850 | 差异=1.2593% -> BUG
512480 半导体ETF:  东财缓存收盘=1.0200(2026-08-31) vs 腾讯昨收=1.0580 | 差异=3.5917% -> BUG
513100 纳指ETF:    东财缓存收盘=2.2040(2026-08-31) vs 腾讯昨收=2.2230 | 差异=0.8547% -> BUG

$ python _full_scan.py (全量18个缓存 vs 腾讯昨收)
共 18 个缓存文件，15 个与腾讯昨收差异>=0.1%: 11 个 >0.5% (BUG级), 5 个 0.1~0.5% (WARN)

$ python _em_probe.py (东财 push2his end=20260831 直接探针，确认东财源正确)
510300 2026-08-31 open=4.64 close=4.685 high=4.692 low=4.618 vol=6731265 amp=1.58
512480 2026-08-31 open=1.016 close=1.058 high=1.059 low=1.013 vol=10951932 amp=4.43
513100 2026-08-31 open=2.207 close=2.223 high=2.229 low=2.198 vol=1332355 amp=1.39
159915 2026-08-31 open=3.389 close=3.456 high=3.456 low=3.377 vol=14205665 amp=2.29

$ 百度K线核实 (512480/510300/513100 08-31 close = 1.06/4.69/2.22，与东财/腾讯一致)

结构性证据: 512480 缓存 08-31 bar high=1.034 < 真实收盘 1.058 —— 真实日K的 high 必 >= close，
缓存 bar 只覆盖了 08-31 上午 ~10:56 之前的交易区间 → 盘中快照，非完整日K
```

## 自动修复记录
- **修复时间**: 2026-09-01T13:20+08:00
- **修复方式**: 对全部 18 个受影响缓存执行确定性重拉。**注意**：直接 `get_kline(code, start='2012-05-28', end='2026-08-31', refresh=True)` 全量重拉时，历史较长标的的长区间请求被东财 push2his 断开（RemoteDisconnected，数据量/风控），而 `get_kline` 对 API 异常静默回退读缓存 → 缓存未更新且无报错。实际采用**月内增量重拉**方案：显式走 `eastmoney_kline(code, start='20260801', end='20260831')`（响应小、稳定，失败自动重试 3 次、间隔 2s），复用 data.py 的 merge 逻辑（concat + `duplicated(keep='last')`）写回 parquet，完整 08-31 bar 覆盖盘中快照。512480/510180/601398 首轮被风控，冷却 45s 后单独重试成功
- **修复结果**: ✅ 修复成功 — 18/18 缓存 08-31 close 与东财/腾讯/百度三源真实收盘价一致（diff=0.0%），且 `high >= close` 结构性检查全部通过（完整日K，非盘中快照）
- **验证**: 全量扫描 18 只缓存 → 0 MISMATCH；`pytest tests/` 全量回归通过（见 BUG-017 自动修复记录）

## 流程根因（建议后续 REQ 关注）
缓存 refresh 发生在交易时段（09:37/10:53/14:10），收盘后（15:00+）无刷新任务兜底 → 盘中快照 bar 成为"最后日K"。建议在数据刷新入口（data.py get_kline 或调度）增加"非交易时段才写当日 bar / 收盘后强制补全当日 bar"的防护；另建议 `get_kline` API 异常回退缓存时打印 WARN（当前静默回退掩盖了长区间重拉失败）。
