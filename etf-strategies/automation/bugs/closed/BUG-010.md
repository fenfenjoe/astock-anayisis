# BUG-010: conftest.py 预置 CLOUD_RESTORE_ON_START=0 破坏 test_load_env.py 断言（干净环境必失败）

- **组件**: tests
- **严重程度**: MEDIUM
- **违反的正确性定义**: 无直接对应 DEF；违反 `tests/test_load_env.py::test_loads_keys_and_does_not_override_existing` 的契约（load_env_file 对未设置键应加载 .env 值）
- **状态**: FIXED
- **auto_fix_eligible**: true
  - 判定理由：测试/配置冲突问题，修复为机械性确定性改动（删除/调整 conftest 预置行或同步修正测试），无算法判断
- **发现时间**: 2026-08-31T12:28 CST
- **发现方式**: 每日代码巡检（全量测试 1 failed 定位）
- **预期行为**: `tests/test_load_env.py::test_loads_keys_and_does_not_override_existing` 验证：`CLOUD_RESTORE_ON_START` 在环境中未设置时，`load_env_file()` 应从 `.env` 加载为 `"1"`（`assert os.environ["CLOUD_RESTORE_ON_START"] == "1"`）
- **实际行为**: `tests/conftest.py` L22（2026-08-31 事故修复，未提交）在 pytest 进程最早阶段执行 `os.environ.setdefault("CLOUD_RESTORE_ON_START", "0")`。由于 conftest 先于任何测试模块导入，测试运行时该键已被预置为 `"0"`，`load_env_file()` 按"不覆盖已存在键"语义跳过 → 测试断言 `== "1"` 永远失败。**在完全干净的环境下实测同样失败**（与宿主环境变量无关，属 conftest 引入的确定性回归）
- **复现步骤**:
  1. `cd etf-strategies && Remove-Item Env:DB_MODE,Env:CLOUD_RESTORE_ON_START,Env:DSH_TEST -ErrorAction SilentlyContinue; python -m pytest tests/test_load_env.py -v --tb=short` → `test_loads_keys_and_does_not_override_existing FAILED`（`assert '0' == '1'`）
  2. 全量运行 `python -m pytest tests/ -q` → 400 passed, 1 failed（即本测试）, 2 skipped
- **受影响文件**:
  - `etf-strategies/tests/conftest.py:22`（`os.environ.setdefault("CLOUD_RESTORE_ON_START", "0")` 与 test_load_env 契约冲突）
- **潜在影响**: 全量测试套件恒定 1 failed（在干净与继承环境均复现），CI/巡检把测试环境回归误判为代码 BUG；同时掩盖其他真实失败信号
- **建议修复**: 二选一（推荐 a）：
  (a) 从 `conftest.py` 删除 `CLOUD_RESTORE_ON_START` 预置行 —— 该防护在 `DB_MODE=file` 强制后已冗余（`cloud_restore/cloud_backup` 均先检查 `USE_MEMORY`，file 模式下 no-op），且 `DSH_TEST=1` + `app.lifespan` 测试免疫已覆盖启动副作用
  (b) 若保留预置，需同步修改 `test_load_env.py` 先 `monkeypatch.delenv("CLOUD_RESTORE_ON_START")` 再调用 load_env_file，并注释说明 conftest 预置背景

## 巡检证据
```
$ Remove-Item Env:DB_MODE,Env:CLOUD_RESTORE_ON_START,Env:DSH_TEST -ErrorAction SilentlyContinue
$ python -m pytest tests/test_load_env.py -v --tb=short
________________ test_loads_keys_and_does_not_override_existing ________________
tests\test_load_env.py:21: in test_loads_keys_and_does_not_override_existing
    assert os.environ["CLOUD_RESTORE_ON_START"] == "1"
E   AssertionError: assert '0' == '1'
========================= 1 failed, 2 passed in 0.32s =========================

$ python -m pytest tests/ -q --cov=backtest --cov-report=term   # file 模式全量
======= 1 failed, 400 passed, 2 skipped, 1 warning in 71.64s ========
FAILED tests/test_load_env.py::test_loads_keys_and_does_not_override_existing
```

## 自动修复判定
- **判定时间**: 2026-08-31T12:35+08:00
- **判定结果**: 自动修复 — 测试/配置冲突，机械性确定性修复
- **建议**: 修复后重跑 `python -m pytest tests/ -q` 确认 400+ passed, 0 failed

## 自动修复记录
- **修复时间**: 2026-08-31T14:12+08:00
- **修复方式**: 采用建议修复方案 (b)：`tests/conftest.py` 对 `CLOUD_RESTORE_ON_START` 强制赋值 `"0"`（维持 test_test_isolation 防护断言在任意宿主环境下成立），并在 `tests/test_load_env.py::test_loads_keys_and_does_not_override_existing` 中先 `monkeypatch.delenv("CLOUD_RESTORE_ON_START")` 再调用 load_env_file，恢复对"未设置键从 .env 加载"语义的验证。未采用方案 (a)（删除预置）的原因：宿主环境 CLOUD_RESTORE_ON_START=1 时 `test_conftest_sets_safe_env` 的 `in ("", "0", "false")` 断言会失败，预置保留可让隔离防护断言确定性成立
- **修复结果**: ✅ 修复成功 — test_load_env `assert "1"` 通过（干净/继承环境均成立），test_test_isolation 防护断言通过
- **验证**: tests/test_load_env.py + tests/test_test_isolation.py → 8 passed；全量回归 400+ passed, 0 failed（见下）
